namespace CoffeePeek.Shops.Domain.Aggregates.CoffeeShopAggregate;

/// <summary>
/// Shop price level: cheap / mid / expensive (дешёвый / средний / дорогой).
/// </summary>
public enum PriceRange
{
    Cheap = 1,
    Moderate = 2,
    Expensive = 3
}
