---
phase: 05-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - CoffeePeek.Account.Application.Tests/Features/User/UpdateUserProfile/UpdateEmail/UpdateEmailRequestHandlerTest.cs
requirements:
  - TEST-05

must_haves:
  truths:
    - "UpdateEmailRequestHandler возвращает успешный ответ и UserRegisteredInternalEvent при смене email"
    - "UpdateEmailRequestHandler бросает NotFoundException когда пользователь не найден"
    - "UpdateEmailRequestHandler бросает DomainException когда email занят другим пользователем"
    - "UpdateEmailRequestHandler успешно обновляет email если GetByEmail возвращает того же пользователя (идемпотентность)"
    - "TEST-05 deferred item из Phase 2 уже закрыт: AuthServiceTests зелёные (141 passed)"
  artifacts:
    - path: "CoffeePeek.Account.Application.Tests/Features/User/UpdateUserProfile/UpdateEmail/UpdateEmailRequestHandlerTest.cs"
      provides: "4 теста для UpdateEmailRequestHandler"
      exports: ["Handle_WhenUserExists_AndEmailFree_UpdatesEmailAndReturnsSuccess",
                "Handle_WhenUserNotFound_ThrowsNotFoundException",
                "Handle_WhenEmailTakenByAnotherUser_ThrowsDomainException",
                "Handle_WhenEmailBelongsToSameUser_UpdatesSuccessfully"]
  key_links:
    - from: "UpdateEmailRequestHandlerTest.cs"
      to: "CoffeePeek.Account.Application/Features/User/UpdateUserProfile/UpdateEmail/UpdateProfileEmailHandler.cs"
      via: "UpdateEmailRequestHandler.Handle(...) прямой вызов"
      pattern: "UpdateEmailRequestHandler\\.Handle"
---

<objective>
Добавить unit-тесты для UpdateEmailRequestHandler в существующий проект CoffeePeek.Account.Application.Tests. Класс обработчика — UpdateEmailRequestHandler (не UpdateProfileEmailHandler!) в namespace CoffeePeek.Account.Application.Features.User.UpdateUserProfile.UpdateEmail. TEST-05 из deferred-items Phase 2 уже закрыт — AuthServiceTests переписаны и 141 тест зелёный; этот план только добавляет недостающее покрытие email-хэндлера.

Purpose: TEST-05 требует тест для UpdateEmail хэндлера; AuthServiceTests-часть уже выполнена.
Output: 1 новый файл с 4 тестами; `dotnet test CoffeePeek.Account.Application.Tests` остаётся зелёным.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-test-coverage/05-RESEARCH.md

<interfaces>
<!-- Из UpdateProfileEmailHandler.cs -->
```csharp
namespace CoffeePeek.Account.Application.Features.User.UpdateUserProfile.UpdateEmail;

public class UpdateEmailRequestHandler
{
    public static async Task<(UpdateEntityResponse<string>, UserRegisteredInternalEvent)> Handle(
        UpdateProfileEmailCommand request,
        IUserRepository userRepository,
        IUnitOfWork unitOfWork,
        CancellationToken ct)
    // Логика:
    // 1. GetById(request.UserId) → null → throw NotFoundException("User not found")
    // 2. GetByEmail(request.Email) → existingOwner.Id != request.UserId → throw DomainException("Email is already taken")
    // 3. user.Credentials.UpdateEmail(request.Email)  — вызывает ResetEmailConfirmedFlow(): EmailConfirmed=false, новый EmailConfirmationToken
    // 4. userRepository.Update(user), unitOfWork.SaveChangesAsync(ct)
    // 5. return (UpdateEntityResponse<string>.Success(...), new UserRegisteredInternalEvent(userId, email, username, token))
}

public record UpdateProfileEmailCommand([property: JsonIgnore] Guid UserId, string Email);
```

<!-- Из Account.Application.Tests — паттерн создания пользователя (из AuthServiceTests.cs) -->
```csharp
// GlobalUsings: global using DomainUser = CoffeePeek.Account.Domain.Entities.UserAggregate.User;
private static DomainUser CreateConfirmedUser(string email = "user@example.com", string hash = "hashed")
{
    var role = Role.Create("User");
    var user = DomainUser.Register(email, "testuser", hash, role);
    typeof(UserCredential)
        .GetProperty(nameof(UserCredential.EmailConfirmed))!
        .SetValue(user.Credentials, true);
    return user;
}
```

<!-- UpdateEmailRequestHandler возвращает кортеж — паттерн из UpdateUsernameHandlerTest.cs -->
```csharp
var (response, @event) = await UpdateEmailRequestHandler.Handle(command, repo.Object, uow.Object, ct);
response.IsSuccess.Should().BeTrue();
@event.Email.Should().Be(newEmail);
@event.ConfirmationToken.Should().NotBeNull();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Тесты UpdateEmailRequestHandler</name>
  <files>
    CoffeePeek.Account.Application.Tests/Features/User/UpdateUserProfile/UpdateEmail/UpdateEmailRequestHandlerTest.cs
  </files>
  <action>
Создать файл в namespace CoffeePeek.Account.Application.Tests.Features.User.UpdateUserProfile.UpdateEmail.

ВАЖНО: класс называется UpdateEmailRequestHandler, не UpdateProfileEmailHandler.

Добавить using (явно, кроме Xunit из GlobalUsings):
- System
- System.Threading
- System.Threading.Tasks
- CoffeePeek.Account.Application.Features.User.UpdateUserProfile.UpdateEmail
- CoffeePeek.Account.Domain
- CoffeePeek.Account.Domain.Entities.RoleAggregate
- CoffeePeek.Account.Domain.Entities.UserAggregate
- CoffeePeek.Shared.Kernel
- CoffeePeek.Shared.Kernel.Exceptions
- FluentAssertions
- Moq

DomainUser доступен через GlobalUsings (global using DomainUser = CoffeePeek.Account.Domain.Entities.UserAggregate.User).

Поля класса:
```
private readonly Mock<IUserRepository> _userRepositoryMock = new();
private readonly Mock<IUnitOfWork> _unitOfWorkMock = new();
private readonly CancellationToken _ct = CancellationToken.None;
```

Вспомогательный метод CreateUser(Guid userId, string email):
- var role = Role.Create("User");
- var user = DomainUser.Register(email, "testuser", "hash", role);
- Установить Id через рефлексию (по паттерну из UpdateUsernameHandlerTest):
  var aggregateRootType = typeof(CoffeePeek.Shared.Domain.Entities.AggregateRoot<Guid>);
  var idProperty = aggregateRootType.GetProperty("Id", System.Reflection.BindingFlags.Public | System.Reflection.BindingFlags.Instance);
  idProperty?.SetValue(user, userId);
- return user;

Тест 1: Handle_WhenUserExists_AndEmailFree_UpdatesEmailAndReturnsSuccess
- userId = Guid.NewGuid(); newEmail = "new@example.com";
- command = new UpdateProfileEmailCommand(userId, newEmail);
- user = CreateUser(userId, "old@example.com");
- _userRepositoryMock.Setup(r => r.GetById(userId, _ct)).ReturnsAsync(user);
- _userRepositoryMock.Setup(r => r.GetByEmail(newEmail, _ct)).ReturnsAsync((DomainUser?)null);
- var (response, @event) = await UpdateEmailRequestHandler.Handle(command, _userRepositoryMock.Object, _unitOfWorkMock.Object, _ct);
- response.IsSuccess.Should().BeTrue();
- @event.UserId.Should().Be(userId);
- @event.Email.Should().Be(newEmail);
- @event.ConfirmationToken.Should().NotBeNullOrEmpty();
- _userRepositoryMock.Verify(r => r.Update(user, _ct), Times.Once);
- _unitOfWorkMock.Verify(u => u.SaveChangesAsync(_ct), Times.Once);

Тест 2: Handle_WhenUserNotFound_ThrowsNotFoundException
- userId = Guid.NewGuid(); command = new UpdateProfileEmailCommand(userId, "any@example.com");
- _userRepositoryMock.Setup(r => r.GetById(userId, _ct)).ReturnsAsync((DomainUser?)null);
- Func<Task> act = async () => await UpdateEmailRequestHandler.Handle(command, _userRepositoryMock.Object, _unitOfWorkMock.Object, _ct);
- await act.Should().ThrowAsync<NotFoundException>().WithMessage("User not found");

Тест 3: Handle_WhenEmailTakenByAnotherUser_ThrowsDomainException
- userId = Guid.NewGuid(); otherUserId = Guid.NewGuid(); newEmail = "taken@example.com";
- command = new UpdateProfileEmailCommand(userId, newEmail);
- user = CreateUser(userId, "old@example.com");
- otherUser = CreateUser(otherUserId, newEmail); // другой пользователь с тем же email
- _userRepositoryMock.Setup(r => r.GetById(userId, _ct)).ReturnsAsync(user);
- _userRepositoryMock.Setup(r => r.GetByEmail(newEmail, _ct)).ReturnsAsync(otherUser);
- Func<Task> act = async () => await UpdateEmailRequestHandler.Handle(command, _userRepositoryMock.Object, _unitOfWorkMock.Object, _ct);
- await act.Should().ThrowAsync<DomainException>().WithMessage("Email is already taken");

Тест 4: Handle_WhenEmailBelongsToSameUser_UpdatesSuccessfully (идемпотентность)
- userId = Guid.NewGuid(); sameEmail = "same@example.com";
- command = new UpdateProfileEmailCommand(userId, sameEmail);
- user = CreateUser(userId, "old@example.com");
- sameUser = CreateUser(userId, sameEmail); // GetByEmail возвращает того же пользователя (одинаковый Id)
- _userRepositoryMock.Setup(r => r.GetById(userId, _ct)).ReturnsAsync(user);
- _userRepositoryMock.Setup(r => r.GetByEmail(sameEmail, _ct)).ReturnsAsync(sameUser);
- Func<Task> act = async () => await UpdateEmailRequestHandler.Handle(command, _userRepositoryMock.Object, _unitOfWorkMock.Object, _ct);
- await act.Should().NotThrowAsync(); // existingOwner.Id == request.UserId → не бросает DomainException
  </action>
  <verify>
    <automated>cd /home/arseny/RiderProjects/CoffeePeek-NET && dotnet test CoffeePeek.Account.Application.Tests 2>&1 | tail -10</automated>
  </verify>
  <done>
    - UpdateEmailRequestHandlerTest.cs создан с 4 тестами
    - `dotnet test CoffeePeek.Account.Application.Tests` → все тесты зелёные (было 141 + 4 новых = 145)
    - Никаких изменений в продакшн-коде
  </done>
</task>

</tasks>

<verification>
dotnet test CoffeePeek.Account.Application.Tests — все 145 тестов зелёные (141 существующих + 4 новых), 0 failed
Проверить что AuthServiceTests тоже зелёные (были 141 — deferred item уже закрыт)
</verification>

<success_criteria>
- `dotnet test CoffeePeek.Account.Application.Tests` → 0 failed
- Handle_WhenUserExists_AndEmailFree_UpdatesEmailAndReturnsSuccess проверяет: response.IsSuccess, event.Email, event.ConfirmationToken, Update вызван, SaveChangesAsync вызван
- Handle_WhenUserNotFound_ThrowsNotFoundException — NotFoundException с сообщением "User not found"
- Handle_WhenEmailTakenByAnotherUser_ThrowsDomainException — DomainException с сообщением "Email is already taken"
- Handle_WhenEmailBelongsToSameUser_UpdatesSuccessfully — нет исключений при existingOwner.Id == request.UserId
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage/05-02-SUMMARY.md` when done
</output>
