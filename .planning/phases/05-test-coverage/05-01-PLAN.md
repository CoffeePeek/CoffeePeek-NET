---
phase: 05-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - CoffeePeek.Shops.Domain.Tests/Aggregates/ReviewAggregate/ReviewTests.cs
  - CoffeePeek.Shops.Domain.Tests/Entities/RatingTests.cs
  - CoffeePeek.Shops.Domain.Tests/Aggregates/CheckInAggregate/CheckInTests.cs
  - CoffeePeek.Shops.Domain.Tests/Aggregates/CoffeeShopAggregate/CoffeeShopTests.cs
requirements:
  - TEST-02

must_haves:
  truths:
    - "Review.Create бросает DomainException при каждом нарушении инварианта (9 случаев)"
    - "Rating.Create бросает DomainException при coffee/place/service за пределами 1–5"
    - "CheckIn.Create бросает DomainException при пустом userId или shopId"
    - "CheckIn.UpdateNote обрезает пробелы и принимает null"
    - "CoffeeShop конструктор устанавливает Name, CreatorId, Status=Active"
    - "CoffeeShop.IsOpen возвращает true при Active-статусе без расписания"
    - "CoffeeShop.AddEquipment не добавляет дубликат по Brand+ModelName"
  artifacts:
    - path: "CoffeePeek.Shops.Domain.Tests/Aggregates/ReviewAggregate/ReviewTests.cs"
      provides: "9 тестов на Review.Create инварианты"
    - path: "CoffeePeek.Shops.Domain.Tests/Entities/RatingTests.cs"
      provides: "7 тестов на Rating.Create инварианты"
    - path: "CoffeePeek.Shops.Domain.Tests/Aggregates/CheckInAggregate/CheckInTests.cs"
      provides: "4 теста на CheckIn.Create и UpdateNote"
    - path: "CoffeePeek.Shops.Domain.Tests/Aggregates/CoffeeShopAggregate/CoffeeShopTests.cs"
      provides: "3 теста на CoffeeShop поведение"
  key_links:
    - from: "ReviewTests.cs"
      to: "CoffeePeek.Shops.Domain/Aggregates/ReviewAggregate/ReviewAction.cs"
      via: "Review.Create вызов с вариацией параметров"
      pattern: "Review\\.Create"
    - from: "RatingTests.cs"
      to: "CoffeePeek.Shops.Domain/Entities/Rating.cs"
      via: "Rating.Create вызов с boundary values"
      pattern: "Rating\\.Create"
---

<objective>
Покрыть юнит-тестами доменную модель Shops: инварианты Review.Create, Rating.Create, CheckIn.Create и поведение агрегата CoffeeShop. Тесты размещаются в уже существующем проекте CoffeePeek.Shops.Domain.Tests, который содержит 12 зелёных тестов и имеет прямую ссылку на CoffeePeek.Shops.Domain без Moq (домен без внешних зависимостей).

Purpose: TEST-02 требует unit-тесты для Shops Domain — фиксирует контракты доменных инвариантов как регрессионную сетку.
Output: 4 новых файла с 23 тестами; все тесты зелёные при `dotnet test CoffeePeek.Shops.Domain.Tests`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-test-coverage/05-RESEARCH.md

<interfaces>
<!-- Из CoffeePeek.Shops.Domain/Aggregates/ReviewAggregate/ReviewAction.cs -->
```csharp
public static Review Create(Guid shopId, Guid userId, string userName, string header,
    string comment, int ratingPlace, int ratingService, int ratingCoffee)
// Бросает DomainException если:
//   shopId == Guid.Empty           → "CoffeeShopId cannot be empty."
//   userId == Guid.Empty           → "UserId cannot be empty."
//   header пустой/whitespace       → "Review header is required."
//   header.Length < 3 или > 100    → "header must be between 3 and 100 characters."
//   comment пустой/whitespace      → "Review comment is required."
//   comment.Length < 10 или > 1000 → "comment must be between 10 and 1000 characters."
```

<!-- Из CoffeePeek.Shops.Domain/Entities/Rating.cs -->
```csharp
public static Rating Create(int place, int service, int coffee)
// Бросает DomainException если coffee/place/service < 1 (MinReviewRate) или > 5 (MaxReviewRate)
// AverageRating = (Place + Place + Service) / 3m   // KNOWN BUG: дважды Place вместо Coffee+Place+Service
```

<!-- Из CoffeePeek.Shops.Domain/Aggregates/CheckInAggregate/CheckInAction.cs -->
```csharp
public static CheckIn Create(Guid userId, Guid shopId, DateTime visitedAt)
// Бросает DomainException: "UserId is required." или "ShopId is required."
public void UpdateNote(string? newNote)  // Note = newNote?.Trim()
```

<!-- Из CoffeePeek.Shops.Domain/Aggregates/CoffeeShopAggregate/CoffeeShop.cs -->
```csharp
public CoffeeShop(Guid creatorId, string name, string? description, PriceRange priceRange, Guid moderationId)
// Id = Guid.NewGuid(); Status = CoffeeShopStatus.Active (по умолчанию из свойства)
public bool IsOpen => IsOpenAt(DateTime.UtcNow)
// Active + нет расписания → true; PermanentlyClosed/TemporarilyClosed → false
public void AddEquipment(Equipment equipment)
// Не добавляет если _equipments.Any(e => e.Brand == equipment.Brand && e.ModelName == equipment.ModelName)
```

<!-- Из CoffeePeek.Shops.Domain.Tests/Entities/CoffeeShopAggregate/EquipmentTest.cs — паттерн тестов -->
```csharp
// Namespace: CoffeePeek.Shops.Domain.Tests.{путь}
// GlobalUsings: только xUnit; FluentAssertions и другие using — явно в каждом файле
// [TestSubject(typeof(...))] атрибут из JetBrains.Annotations
// Нет Moq — домен без зависимостей
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review.Create и Rating.Create unit-тесты</name>
  <files>
    CoffeePeek.Shops.Domain.Tests/Aggregates/ReviewAggregate/ReviewTests.cs
    CoffeePeek.Shops.Domain.Tests/Entities/RatingTests.cs
  </files>
  <action>
Создать два файла с юнит-тестами для доменных инвариантов.

Файл ReviewTests.cs в namespace CoffeePeek.Shops.Domain.Tests.Aggregates.ReviewAggregate:
- Добавить using: FluentAssertions, JetBrains.Annotations, CoffeePeek.Shared.Kernel.Exceptions, CoffeePeek.Shops.Domain.Aggregates.ReviewAggregate
- Аннотировать [TestSubject(typeof(Review))]
- Вспомогательные константы для valid data: validShopId=Guid.NewGuid(), validUserId=Guid.NewGuid(), validHeader="Valid Header", validComment="Valid comment text", validRating=3
- Написать 9 методов [Fact]:
  1. Create_WithEmptyShopId_ThrowsDomainException — Review.Create(Guid.Empty, validUserId, "user", validHeader, validComment, 3, 3, 3); ожидать ThrowAsync<DomainException> (или Throw<DomainException> — метод синхронный, можно Action)
  2. Create_WithEmptyUserId_ThrowsDomainException — shopId=Guid.NewGuid(), userId=Guid.Empty
  3. Create_WithBlankHeader_ThrowsDomainException — header=" "
  4. Create_WithHeaderTooShort_ThrowsDomainException — header="ab" (2 символа < MinReviewHeaderLength=3)
  5. Create_WithHeaderTooLong_ThrowsDomainException — header=new string('a', 101) (> MaxReviewHeaderLength=100)
  6. Create_WithBlankComment_ThrowsDomainException — comment="   "
  7. Create_WithCommentTooShort_ThrowsDomainException — comment="Short" (5 символов < MinReviewCommentLength=10)
  8. Create_WithCommentTooLong_ThrowsDomainException — comment=new string('a', 1001) (> MaxReviewCommentLength=1000)
  9. Create_WithValidData_ReturnsReviewWithAllFields — все валидные данные; проверить review.CoffeeShopId, review.UserId, review.Header, review.Comment не null/empty; review.Rating не null

Для тестов-исключений паттерн: Action act = () => Review.Create(...); act.Should().Throw<DomainException>();
Review.Create синхронный — не async.

Файл RatingTests.cs в namespace CoffeePeek.Shops.Domain.Tests.Entities:
- Добавить using: FluentAssertions, JetBrains.Annotations, CoffeePeek.Shared.Kernel.Exceptions, CoffeePeek.Shops.Domain.Entities
- Аннотировать [TestSubject(typeof(Rating))]
- Написать 7 методов [Fact]:
  1. Create_WithCoffeeBelowMin_ThrowsDomainException — coffee=0 (MinReviewRate=1)
  2. Create_WithCoffeeAboveMax_ThrowsDomainException — coffee=6 (MaxReviewRate=5)
  3. Create_WithPlaceBelowMin_ThrowsDomainException — place=0
  4. Create_WithPlaceAboveMax_ThrowsDomainException — place=6
  5. Create_WithServiceBelowMin_ThrowsDomainException — service=0
  6. Create_WithServiceAboveMax_ThrowsDomainException — service=6
  7. Create_WithAllValidValues_ReturnsRating — place=3, service=4, coffee=5; проверить rating.Place==3, rating.Service==4, rating.Coffee==5

ВАЖНО о баге AverageRating: НЕ проверять семантическую корректность AverageRating. Добавить комментарий в тест 7:
// NOTE: AverageRating = (Place + Place + Service) / 3m — known bug (Coffee ignored). Not asserting AverageRating here.

Паттерн для исключений в Rating: Action act = () => Rating.Create(place, service, coffee); act.Should().Throw<DomainException>();
  </action>
  <verify>
    <automated>cd /home/arseny/RiderProjects/CoffeePeek-NET && dotnet build CoffeePeek.Shops.Domain.Tests --no-restore -v q 2>&1 | tail -5 && dotnet test CoffeePeek.Shops.Domain.Tests --no-build --filter "ReviewTests|RatingTests" -v n 2>&1 | tail -15</automated>
  </verify>
  <done>
    - ReviewTests.cs содержит 9 [Fact] методов, все проходят
    - RatingTests.cs содержит 7 [Fact] методов, все проходят
    - Нет компиляционных ошибок
  </done>
</task>

<task type="auto">
  <name>Task 2: CheckIn.Create и CoffeeShop unit-тесты</name>
  <files>
    CoffeePeek.Shops.Domain.Tests/Aggregates/CheckInAggregate/CheckInTests.cs
    CoffeePeek.Shops.Domain.Tests/Aggregates/CoffeeShopAggregate/CoffeeShopTests.cs
  </files>
  <action>
Создать два файла.

Файл CheckInTests.cs в namespace CoffeePeek.Shops.Domain.Tests.Aggregates.CheckInAggregate:
- using: FluentAssertions, JetBrains.Annotations, CoffeePeek.Shared.Kernel.Exceptions, CoffeePeek.Shops.Domain.Aggregates.CheckInAggregate
- [TestSubject(typeof(CheckIn))]
- 4 метода [Fact]:
  1. Create_WithEmptyUserId_ThrowsDomainException — CheckIn.Create(Guid.Empty, Guid.NewGuid(), DateTime.UtcNow.AddHours(-1)); Action act = () => ...; act.Should().Throw<DomainException>().WithMessage("UserId is required.");
  2. Create_WithEmptyShopId_ThrowsDomainException — CheckIn.Create(Guid.NewGuid(), Guid.Empty, DateTime.UtcNow.AddHours(-1)); аналогично "ShopId is required."
  3. Create_WithValidData_ReturnsCheckInWithAllFields — оба guid не пустые; проверить checkIn.UserId, checkIn.ShopId, checkIn.VisitedAt; checkIn.Id != Guid.Empty
  4. UpdateNote_WithValue_SetsNote — создать валидный CheckIn; вызвать checkIn.UpdateNote("  trim  "); checkIn.Note.Should().Be("trim")

Файл CoffeeShopTests.cs в namespace CoffeePeek.Shops.Domain.Tests.Aggregates.CoffeeShopAggregate:
- using: FluentAssertions, JetBrains.Annotations, CoffeePeek.Shops.Domain.Aggregates.CoffeeShopAggregate, CoffeePeek.Shops.Domain.Entities (для Equipment, EquipmentCategory), CoffeePeek.Shops.Domain.Aggregates.CoffeeShopAggregate.Enums (для CoffeeShopStatus, PriceRange)
- [TestSubject(typeof(CoffeeShop))]
- 3 метода [Fact]:
  1. Constructor_WithValidData_SetsProperties — new CoffeeShop(creatorId, "Test Shop", null, PriceRange.Medium, Guid.NewGuid()); проверить shop.Name=="Test Shop", shop.CreatorId==creatorId, shop.Id != Guid.Empty, shop.Status==CoffeeShopStatus.Active
  2. IsOpen_WhenActiveWithNoSchedule_ReturnsTrue — создать магазин конструктором; shop.IsOpen.Should().BeTrue(); (Active + Schedules.Count==0 → true по логике IsOpenAt)
  3. AddEquipment_WithDuplicateBrandAndModel_DoesNotAddTwice — создать EquipmentCategory eq = new EquipmentCategory(); создать two equipment с одинаковыми Brand="Brand" и ModelName="Model": var e1 = new Equipment("Brand", "Model", eq); var e2 = new Equipment("Brand", "Model", eq); вызвать shop.AddEquipment(e1); shop.AddEquipment(e2); shop.Equipments.Count.Should().Be(1)

ЗАМЕТКА по PriceRange: импортировать из CoffeePeek.Shops.Domain.Aggregates.CoffeeShopAggregate или из CoffeePeek.Contract.Enums — проверить по using в CoffeeShop.cs (там нет using Contract, значит PriceRange определён в домене). Найти namespace через grep или проверить: в CoffeeShop.cs нет using для PriceRange, значит он в том же namespace или CoffeePeek.Shops.Domain.Aggregates.CoffeeShopAggregate. Если не найдёт — добавить using CoffeePeek.Contract.Enums как fallback.
  </action>
  <verify>
    <automated>cd /home/arseny/RiderProjects/CoffeePeek-NET && dotnet test CoffeePeek.Shops.Domain.Tests 2>&1 | tail -10</automated>
  </verify>
  <done>
    - CheckInTests.cs: 4 теста, все зелёные
    - CoffeeShopTests.cs: 3 теста, все зелёные
    - Весь CoffeePeek.Shops.Domain.Tests проходит (было 12 тестов + 16 новых = 28 итого)
  </done>
</task>

</tasks>

<verification>
dotnet test CoffeePeek.Shops.Domain.Tests — все тесты зелёные, 0 failed
Новые тест-файлы не содержат Moq (не нужен для domain-only тестов)
Namespace в файлах соответствует путям директорий (паттерн проекта)
</verification>

<success_criteria>
- `dotnet test CoffeePeek.Shops.Domain.Tests` → 28 passed (12 existing + 16 new), 0 failed
- ReviewTests: 9 тестов покрывают все инварианты из ReviewAction.cs
- RatingTests: 7 тестов покрывают boundary values MinReviewRate=1 и MaxReviewRate=5; баг AverageRating задокументирован комментарием
- CheckInTests: 4 теста покрывают CheckIn.Create и UpdateNote
- CoffeeShopTests: 3 теста покрывают конструктор, IsOpen, AddEquipment дедупликацию
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage/05-01-SUMMARY.md` when done
</output>
