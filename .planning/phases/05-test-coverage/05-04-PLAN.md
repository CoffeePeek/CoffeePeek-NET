---
phase: 05-test-coverage
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
autonomous: true
files_modified:
  - CoffeePeek.Shops.Application/Features/CheckIn/CreateCheckIn/CreateCheckInHandler.cs
  - CoffeePeek.Shops.Application/ValidationStrategy/CheckIn/CheckInValidationStrategy.cs
  - CoffeePeek.Shops.Application.Tests/Features/CheckIn/CreateCheckInHandlerTests.cs
requirements:
  - TEST-01
  - TEST-04

must_haves:
  truths:
    - "CreateCheckInHandler.cs строка 74: catch (DomainException) { /* ignore */ } заменён на throw"
    - "CheckInValidationStrategy: условие Rating < 0 изменено на < 1 (MinReviewRate)"
    - "Handle_PublicCheckIn_WithInvalidRating_ThrowsDomainException проходит (TEST-04 закрыт)"
    - "Handle_PrivateCheckIn_CreatesCheckInAndSaves не вызывает bus.PublishAsync"
    - "Handle_PublicCheckIn_WithValidRating_PublishesEventAndSaves вызывает bus.PublishAsync"
    - "Handle_WithInvalidCommand_ThrowsValidationException"
  artifacts:
    - path: "CoffeePeek.Shops.Application/Features/CheckIn/CreateCheckIn/CreateCheckInHandler.cs"
      provides: "Исправленный catch-блок: DomainException пробрасывается"
    - path: "CoffeePeek.Shops.Application/ValidationStrategy/CheckIn/CheckInValidationStrategy.cs"
      provides: "Согласованная валидация: Rating >= 1 вместо Rating >= 0"
    - path: "CoffeePeek.Shops.Application.Tests/Features/CheckIn/CreateCheckInHandlerTests.cs"
      provides: "4 теста для CreateCheckInHandler включая TEST-04 regression"
  key_links:
    - from: "CreateCheckInHandlerTests.cs"
      to: "CreateCheckInHandler.cs"
      via: "CreateCheckInHandler.Handle() прямой вызов"
      pattern: "CreateCheckInHandler\\.Handle"
    - from: "Handle_PublicCheckIn_WithInvalidRating_ThrowsDomainException"
      to: "ReviewAction.cs Rating.Create(place=0)"
      via: "DomainException пробрасывается через catch { throw; }"
      pattern: "DomainException"
---

<objective>
Исправить продакшн-баг TEST-04: `catch (DomainException) { /* ignore */ }` в CreateCheckInHandler молча проглатывает невалидный рейтинг — заменить на `throw`. Дополнительно согласовать CheckInValidationStrategy с доменным MinReviewRate=1. Написать 4 теста для CreateCheckInHandler, включая regression-тест для TEST-04.

Purpose: TEST-04 + частично TEST-01 (CreateCheckIn handler). Без этого фикса публичный check-in с невалидным рейтингом возвращал 200 OK при несозданном отзыве.
Output: 2 изменённых продакшн-файла + 1 новый тестовый файл с 4 тестами.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-test-coverage/05-RESEARCH.md

<interfaces>
<!-- Из CreateCheckInHandler.cs — текущий баг и контекст вокруг него -->
```csharp
// Строки 47–79 (сокращённо):
if (command.IsPublic)
{
    queryCheckInRepository.Add(checkIn);

    try
    {
        var commentPreview = string.Join(" ",
            command.Note?.Split(' ', StringSplitOptions.RemoveEmptyEntries).Take(3) ?? []);

        var review = Review.Create(
            command.CoffeeShopId, command.UserId, command.UserName,
            header: commentPreview,
            comment: command.Note!,
            ratingPlace: command.Rating!.Place,
            ratingService: command.Rating.Service,
            ratingCoffee: command.Rating.Coffee);

        await bus.PublishAsync(new CheckinCreatedEvent { ... });
    }
    catch (DomainException) { /* ignore */ }   // ← СТРОКА 74: ЗАМЕНИТЬ НА throw
}

await unitOfWork.SaveChangesAsync(ct);
```

<!-- Из CheckInValidationStrategy.cs — условие требует исправления -->
```csharp
if (command.Rating.Coffee < 0 || command.Rating.Coffee > 5    // ← < 0 должно быть < 1
    || command.Rating.Place < 0                                // ← аналогично
    || command.Rating.Place > 5
    || command.Rating.Service < 0                              // ← аналогично
    || command.Rating.Service > 5)
{
    return ValidationResult.Invalid("Rating must be between 0 and 5");
}
```

<!-- Из CreateCheckInCommand.cs -->
```csharp
public record CreateCheckInCommand(
    [Required] Guid CoffeeShopId,
    [Required] bool IsPublic,
    [Required] DateTime VisitedAt,
    [MaxLength(BusinessConstants.MaxCheckInNoteLength)] string? Note,
    ICollection<UploadedPhotoDto>? Photos,
    RatingDto? Rating)
{
    [JsonIgnore] public Guid UserId { get; init; }
    [JsonIgnore] public string UserName { get; init; } = string.Empty;
}
```

<!-- Моки для CreateCheckInHandler.Handle -->
```csharp
// Wolverine IMessageBus: Mock<IMessageBus>
// _busMock.Setup(b => b.PublishAsync(It.IsAny<CheckinCreatedEvent>())).Returns(Task.CompletedTask);
// После вызова: _busMock.Verify(b => b.PublishAsync(It.IsAny<CheckinCreatedEvent>()), Times.Once);

// IAsyncValidationStrategy<CreateCheckInCommand>:
// _validationMock.Setup(v => v.ValidateAsync(command, _ct)).ReturnsAsync(ValidationResult.Valid);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Продакшн-фикс catch-блока и CheckInValidationStrategy</name>
  <files>
    CoffeePeek.Shops.Application/Features/CheckIn/CreateCheckIn/CreateCheckInHandler.cs
    CoffeePeek.Shops.Application/ValidationStrategy/CheckIn/CheckInValidationStrategy.cs
  </files>
  <action>
Фикс 1 — CreateCheckInHandler.cs строка 74:
Заменить:
    catch (DomainException) { /* ignore */ }
На:
    catch (DomainException)
    {
        throw;
    }

Это единственное изменение в этом файле. Ничего больше не менять.

Фикс 2 — CheckInValidationStrategy.cs:
В методе Validate заменить условие валидации рейтинга:
    if (command.Rating.Coffee < 0 || command.Rating.Coffee > 5
        || command.Rating.Place < 0
        || command.Rating.Place > 5
        || command.Rating.Service < 0
        || command.Rating.Service > 5)
    {
        return ValidationResult.Invalid("Rating must be between 0 and 5");
    }

На:
    if (command.Rating.Coffee < BusinessConstants.MinReviewRate || command.Rating.Coffee > BusinessConstants.MaxReviewRate
        || command.Rating.Place < BusinessConstants.MinReviewRate
        || command.Rating.Place > BusinessConstants.MaxReviewRate
        || command.Rating.Service < BusinessConstants.MinReviewRate
        || command.Rating.Service > BusinessConstants.MaxReviewRate)
    {
        return ValidationResult.Invalid($"Rating must be between {BusinessConstants.MinReviewRate} and {BusinessConstants.MaxReviewRate}");
    }

Это единственное изменение в этом файле. Ничего больше не менять.

Обоснование: BusinessConstants.MinReviewRate=1, поэтому Rating=0 теперь отвергается application-уровнем (а не только domain-уровнем). Тест TEST-04 тестирует сценарий где application validation проходит (мок возвращает Valid), но domain validation падает — это возможно только через прямой мок.
  </action>
  <verify>
    <automated>cd /home/arseny/RiderProjects/CoffeePeek-NET && dotnet build CoffeePeek.Shops.Application --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - CreateCheckInHandler.cs: catch-блок содержит `throw;` вместо `/* ignore */`
    - CheckInValidationStrategy.cs: граница `< 0` заменена на `< BusinessConstants.MinReviewRate` (= 1)
    - Проект компилируется без ошибок
  </done>
</task>

<task type="auto">
  <name>Task 2: CreateCheckInHandlerTests</name>
  <files>
    CoffeePeek.Shops.Application.Tests/Features/CheckIn/CreateCheckInHandlerTests.cs
  </files>
  <action>
Создать файл в namespace CoffeePeek.Shops.Application.Tests.Features.CheckIn.CreateCheckIn.

Явные using:
- System
- System.Threading
- System.Threading.Tasks
- CoffeePeek.Contract.Dtos (RatingDto)
- CoffeePeek.Contract.Events.Shops (CheckinCreatedEvent)
- CoffeePeek.Shared.Kernel (IUnitOfWork)
- CoffeePeek.Shared.Kernel.Exceptions (DomainException, ValidationException)
- CoffeePeek.Shared.Validation (IAsyncValidationStrategy, ValidationResult)
- CoffeePeek.Shops.Application.Features.CheckIn.CreateCheckIn
- CoffeePeek.Shops.Domain.Aggregates.CheckInAggregate (IQueryCheckInRepository)
- FluentAssertions
- MapsterMapper (IMapper)
- Moq
- Wolverine (IMessageBus)

Поля класса:
```
private readonly Mock<IQueryCheckInRepository> _checkInRepoMock = new();
private readonly Mock<IUnitOfWork> _unitOfWorkMock = new();
private readonly Mock<IMessageBus> _busMock = new();
private readonly Mock<IAsyncValidationStrategy<CreateCheckInCommand>> _validationMock = new();
private readonly Mock<IMapper> _mapperMock = new();
private readonly CancellationToken _ct = CancellationToken.None;
```

Вспомогательный метод BuildCommand(bool isPublic, RatingDto? rating = null, string note = "Some valid note here for testing purposes"):
- return new CreateCheckInCommand(
      CoffeeShopId: Guid.NewGuid(),
      IsPublic: isPublic,
      VisitedAt: DateTime.UtcNow.AddHours(-1),
      Note: note,
      Photos: null,
      Rating: rating)
  { UserId = Guid.NewGuid(), UserName = "testuser" };

Тест 1: Handle_PrivateCheckIn_CreatesCheckInAndSaves
- command = BuildCommand(isPublic: false);
- _validationMock.Setup(v => v.ValidateAsync(command, _ct)).ReturnsAsync(ValidationResult.Valid);
- var result = await CreateCheckInHandler.Handle(command, _checkInRepoMock.Object, _unitOfWorkMock.Object, _busMock.Object, _validationMock.Object, _mapperMock.Object, _ct);
- result.IsSuccess.Should().BeTrue();
- _busMock.Verify(b => b.PublishAsync(It.IsAny<CheckinCreatedEvent>()), Times.Never);
- _unitOfWorkMock.Verify(u => u.SaveChangesAsync(_ct), Times.Once);

Тест 2: Handle_PublicCheckIn_WithValidRating_PublishesEventAndSaves
- rating = new RatingDto { Place = 3, Service = 4, Coffee = 5 }; // все в диапазоне 1–5
- command = BuildCommand(isPublic: true, rating: rating, note: "This is a sufficiently long note for review comment");
- _validationMock.Setup(v => v.ValidateAsync(command, _ct)).ReturnsAsync(ValidationResult.Valid);
- _busMock.Setup(b => b.PublishAsync(It.IsAny<CheckinCreatedEvent>())).Returns(Task.CompletedTask);
- _mapperMock.Setup(m => m.Map<CoffeePeek.Contract.Dtos.CoffeeShop.ReviewDto>(It.IsAny<object>())).Returns(new CoffeePeek.Contract.Dtos.CoffeeShop.ReviewDto());
- var result = await CreateCheckInHandler.Handle(command, _checkInRepoMock.Object, _unitOfWorkMock.Object, _busMock.Object, _validationMock.Object, _mapperMock.Object, _ct);
- result.IsSuccess.Should().BeTrue();
- _busMock.Verify(b => b.PublishAsync(It.IsAny<CheckinCreatedEvent>()), Times.Once);
- _unitOfWorkMock.Verify(u => u.SaveChangesAsync(_ct), Times.Once);

Тест 3: Handle_PublicCheckIn_WithInvalidRating_ThrowsDomainException (TEST-04 regression)
- rating = new RatingDto { Place = 0, Service = 3, Coffee = 3 }; // Place=0 нарушает MinReviewRate=1 в Review.Create
- note = "This note is long enough to serve as both a header and comment in the review";
- command = BuildCommand(isPublic: true, rating: rating, note: note);
- _validationMock.Setup(v => v.ValidateAsync(command, _ct)).ReturnsAsync(ValidationResult.Valid); // application validation проходит через мок
- Func<Task> act = async () => await CreateCheckInHandler.Handle(command, _checkInRepoMock.Object, _unitOfWorkMock.Object, _busMock.Object, _validationMock.Object, _mapperMock.Object, _ct);
- await act.Should().ThrowAsync<DomainException>();
- _unitOfWorkMock.Verify(u => u.SaveChangesAsync(It.IsAny<CancellationToken>()), Times.Never);
// Этот тест проходит ТОЛЬКО после фикса catch-блока в Task 1

Тест 4: Handle_WithInvalidCommand_ThrowsValidationException
- command = BuildCommand(isPublic: false);
- _validationMock.Setup(v => v.ValidateAsync(command, _ct)).ReturnsAsync(ValidationResult.Invalid("CoffeeShopId is required"));
- Func<Task> act = async () => await CreateCheckInHandler.Handle(command, _checkInRepoMock.Object, _unitOfWorkMock.Object, _busMock.Object, _validationMock.Object, _mapperMock.Object, _ct);
- await act.Should().ThrowAsync<CoffeePeek.Shared.Kernel.Exceptions.ValidationException>();
- _unitOfWorkMock.Verify(u => u.SaveChangesAsync(It.IsAny<CancellationToken>()), Times.Never);

ЗАМЕТКА по IMessageBus: WolverineFx.IMessageBus — PublishAsync принимает object. Используй It.IsAny<CheckinCreatedEvent>() если метод типизирован, или It.IsAny<object>() если нет. Если не компилируется с generic — заменить на It.IsAny<object>().

ЗАМЕТКА по ReviewDto: полный namespace CoffeePeek.Contract.Dtos.CoffeeShop.ReviewDto. Если есть конфликт имён с другими namespace — используй полный путь в Mock Setup.

ЗАМЕТКА по note для Тест 2: commentPreview берётся как первые 3 слова Note через Split. Для прохождения Review.Create нужен header >= 3 символов (MinReviewHeaderLength) и comment >= 10 символов (MinReviewCommentLength). Note "This is a sufficiently long note for review comment" содержит header "This is a" (9 символов ≥ 3) и comment==Note (47 символов ≥ 10) — валидно.
  </action>
  <verify>
    <automated>cd /home/arseny/RiderProjects/CoffeePeek-NET && dotnet test CoffeePeek.Shops.Application.Tests --filter "CreateCheckInHandlerTests" -v n 2>&1 | tail -15</automated>
  </verify>
  <done>
    - CreateCheckInHandlerTests.cs: 4 теста, все зелёные
    - Тест Handle_PublicCheckIn_WithInvalidRating_ThrowsDomainException подтверждает что DomainException пробрасывается (TEST-04 закрыт)
    - `dotnet test CoffeePeek.Shops.Application.Tests` → все 13 тестов зелёные
  </done>
</task>

</tasks>

<verification>
dotnet test CoffeePeek.Shops.Application.Tests — 13 passed (9 after plan 03 + 4 new), 0 failed
grep в CreateCheckInHandler.cs строка 74 показывает "throw;" а не "/* ignore */"
</verification>

<success_criteria>
- CreateCheckInHandler.cs: catch(DomainException) содержит `throw;`
- CheckInValidationStrategy.cs: граница MinReviewRate=1 вместо 0
- `dotnet test CoffeePeek.Shops.Application.Tests` → 0 failed
- TEST-04 regression: Handle_PublicCheckIn_WithInvalidRating_ThrowsDomainException проходит
- TEST-01 partial: CreateCheckInHandler покрыт 4 тестами
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage/05-04-SUMMARY.md` when done
</output>
