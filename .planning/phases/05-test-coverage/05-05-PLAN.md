---
phase: 05-test-coverage
plan: 05
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
autonomous: true
files_modified:
  - CoffeePeek.Shops.Application.Tests/Features/CoffeeShop/SearchCoffeeShopsHandlerTests.cs
  - CoffeePeek.Shops.Application.Tests/Features/CoffeeShop/GetCoffeeShopHandlerTests.cs
requirements:
  - TEST-01

must_haves:
  truths:
    - "SearchCoffeeShopsHandler возвращает IsSuccess=true для анонимного запроса"
    - "SearchCoffeeShopsHandler обогащает IsFavorite/IsVisited для авторизованного пользователя"
    - "SearchCoffeeShopsHandler возвращает IsSuccess=false когда кэш возвращает null"
    - "GetCoffeeShopHandler возвращает IsSuccess=true для анонимного запроса без персонализации"
    - "GetCoffeeShopHandler проставляет IsFavorite/IsVisited для авторизованного пользователя"
    - "GetCoffeeShopHandler возвращает IsSuccess=false когда кэш возвращает null"
  artifacts:
    - path: "CoffeePeek.Shops.Application.Tests/Features/CoffeeShop/SearchCoffeeShopsHandlerTests.cs"
      provides: "3 теста для SearchCoffeeShopsHandler"
    - path: "CoffeePeek.Shops.Application.Tests/Features/CoffeeShop/GetCoffeeShopHandlerTests.cs"
      provides: "3 теста для GetCoffeeShopHandler"
  key_links:
    - from: "SearchCoffeeShopsHandlerTests.cs"
      to: "SearchCoffeeShopsHandler.cs"
      via: "ICacheService.GetAsync с factory перегрузкой — Pitfall 1 из RESEARCH.md"
      pattern: "ICacheService.*GetAsync.*Func"
    - from: "GetCoffeeShopHandlerTests.cs"
      to: "GetCoffeeShopHandler.cs"
      via: "ICacheService.GetAsync(cacheKey, factory, expiration, ct) — 4 параметра"
      pattern: "GetAsync.*TimeSpan"
---

<objective>
Покрыть тестами два самых сложных хэндлера: SearchCoffeeShopsHandler и GetCoffeeShopHandler — оба используют ICacheService с async factory перегрузкой, которая требует специального Moq setup. После этого плана весь TEST-01 закрыт, а финальный `dotnet test CoffeePeek.slnx` должен пройти.

Purpose: TEST-01 финальная часть — cache-heavy handlers. Без этих тестов TEST-01 остаётся незакрытым.
Output: 2 новых файла с 6 тестами; `dotnet test CoffeePeek.slnx` → 0 failed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-test-coverage/05-RESEARCH.md

<interfaces>
<!-- ICacheService — КРИТИЧНО для mock setup (из Shared.Domain) -->
```csharp
public interface ICacheService
{
    Task<T?> GetAsync<T>(CacheKey cacheKey, CancellationToken cancellationToken = default);
    Task<T?> GetAsync<T>(
        CacheKey cacheKey,
        Func<CancellationToken, Task<T>> factory,
        TimeSpan? expiration = null,
        CancellationToken cancellationToken = default);
    Task SetAsync<T>(CacheKey cacheKey, T value, TimeSpan? customTtl = null);
    Task RemoveAsync(CacheKey cacheKey);
    Task<bool> ExistsAsync(CacheKey cacheKey);
    Task RemoveByPattern(string pattern);
}
```

<!-- SearchCoffeeShopsHandler — вызов кэша БЕЗ expiration параметра -->
```csharp
// Строка 24-34:
var cachedResponse = await redisService.GetAsync(cacheKey, async token =>
{
    var (items, totalCount) = await coffeeShopQueries.Search(queryRequest, token);
    return new GetCoffeeShopsResponse { ... };
}, cancellationToken: ct);  // expiration=null (named param, без TimeSpan)
```

<!-- GetCoffeeShopHandler — вызов кэша С expiration параметром -->
```csharp
// Строка 25-38:
var shopDto = await redisService.GetAsync(cacheKey, async token =>
{
    ...
}, cacheKey.DefaultTtl, ct);  // TimeSpan? expiration = cacheKey.DefaultTtl
```

<!-- Pitfall 1 из RESEARCH.md — ICacheService factory mock -->
```csharp
// SearchCoffeeShopsHandler (expiration = null):
_cacheMock
    .Setup(c => c.GetAsync(
        It.IsAny<CacheKey>(),
        It.IsAny<Func<CancellationToken, Task<GetCoffeeShopsResponse>>>(),
        It.IsAny<TimeSpan?>(),
        It.IsAny<CancellationToken>()))
    .ReturnsAsync(expectedResponse);

// GetCoffeeShopHandler (expiration = cacheKey.DefaultTtl):
_cacheMock
    .Setup(c => c.GetAsync(
        It.IsAny<CacheKey>(),
        It.IsAny<Func<CancellationToken, Task<CoffeeShopDetailsDto>>>(),
        It.IsAny<TimeSpan?>(),
        It.IsAny<CancellationToken>()))
    .ReturnsAsync(shopDetailsDto);
```

<!-- SearchCoffeeShopsHandler зависимости -->
```csharp
public static async Task<Response<GetCoffeeShopsResponse>> Handle(
    SearchCoffeeShopsQuery queryRequest,
    ICoffeeShopQueries coffeeShopQueries,
    IUserFavoriteRepository favoriteRepository,
    IQueryCheckInRepository visitRepository,
    ICacheService redisService,
    CancellationToken ct)
// При null кэше: return Response<GetCoffeeShopsResponse>.Error("Failed to retrieve coffee shop search results")
// При UserId.HasValue: favoriteRepository.GetFavoriteShopIdsAsync + visitRepository.GetVisitedShopIdsAsync
```

<!-- GetCoffeeShopHandler зависимости -->
```csharp
public static async Task<Response<GetCoffeeShopResponse>> Handle(
    GetCoffeeShopQuery query,
    ICoffeeShopQueries shopQueries,
    IUserFavoriteRepository favoriteRepository,
    IQueryCheckInRepository checkInRepository,
    IQueryReviewRepository reviewRepository,
    ICacheService redisService,
    IMapper mapper,
    CancellationToken ct)
// При null кэше: return Response<GetCoffeeShopResponse>.Error("Shop not found")
// При query.UserId.HasValue: favoriteRepository.Exists + checkInRepository.Exists
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SearchCoffeeShopsHandlerTests</name>
  <files>
    CoffeePeek.Shops.Application.Tests/Features/CoffeeShop/SearchCoffeeShopsHandlerTests.cs
  </files>
  <action>
Создать файл в namespace CoffeePeek.Shops.Application.Tests.Features.CoffeeShop.SearchCoffeeShops.

Явные using:
- System
- System.Collections.Generic
- System.Threading
- System.Threading.Tasks
- CoffeePeek.Contract.Dtos.CoffeeShop (ShortShopDto)
- CoffeePeek.Shared.Domain.Interfaces.Infrastructure (ICacheService, CacheKey)
- CoffeePeek.Shops.Application.Common.Responses (GetCoffeeShopsResponse)
- CoffeePeek.Shops.Application.Features.CoffeeShop.GetCoffeeShop (ICoffeeShopQueries)
- CoffeePeek.Shops.Application.Features.CoffeeShop.SearchCoffeeShops
- CoffeePeek.Shops.Domain.Aggregates.CheckInAggregate (IQueryCheckInRepository)
- CoffeePeek.Shops.Domain.Aggregates.UserFavoriteAggregate (IUserFavoriteRepository)
- FluentAssertions
- Moq

Поля класса:
```
private readonly Mock<ICoffeeShopQueries> _shopQueriesMock = new();
private readonly Mock<IUserFavoriteRepository> _favoriteRepoMock = new();
private readonly Mock<IQueryCheckInRepository> _visitRepoMock = new();
private readonly Mock<ICacheService> _cacheMock = new();
private readonly CancellationToken _ct = CancellationToken.None;
```

Вспомогательный метод BuildQuery(Guid? userId = null):
- return new SearchCoffeeShopsQuery(UserId: userId);

Тест 1: Handle_AnonymousRequest_ReturnsCachedResponse
- shopId = Guid.NewGuid();
- var shop = new ShortShopDto { Id = shopId, Name = "Test Shop" };
- expectedResponse = new GetCoffeeShopsResponse { CoffeeShops = [shop], TotalItems = 1, CurrentPage = 1, TotalPages = 1 };
- _cacheMock.Setup(c => c.GetAsync(
      It.IsAny<CacheKey>(),
      It.IsAny<Func<CancellationToken, Task<GetCoffeeShopsResponse>>>(),
      It.IsAny<TimeSpan?>(),
      It.IsAny<CancellationToken>()))
    .ReturnsAsync(expectedResponse);
- query = BuildQuery(userId: null);
- var result = await SearchCoffeeShopsHandler.Handle(query, _shopQueriesMock.Object, _favoriteRepoMock.Object, _visitRepoMock.Object, _cacheMock.Object, _ct);
- result.IsSuccess.Should().BeTrue();
- result.Data.CoffeeShops.Should().HaveCount(1);
- _favoriteRepoMock.Verify(r => r.GetFavoriteShopIdsAsync(It.IsAny<Guid>(), It.IsAny<CancellationToken>()), Times.Never);
- _visitRepoMock.Verify(r => r.GetVisitedShopIdsAsync(It.IsAny<Guid>(), It.IsAny<CancellationToken>()), Times.Never);

Тест 2: Handle_AuthenticatedRequest_EnrichesWithFavoriteAndVisitedFlags
- userId = Guid.NewGuid(); shopId = Guid.NewGuid();
- shop = new ShortShopDto { Id = shopId, Name = "Test Shop" };
- response = new GetCoffeeShopsResponse { CoffeeShops = [shop], TotalItems = 1, CurrentPage = 1, TotalPages = 1 };
- _cacheMock.Setup(c => c.GetAsync(It.IsAny<CacheKey>(), It.IsAny<Func<CancellationToken, Task<GetCoffeeShopsResponse>>>(), It.IsAny<TimeSpan?>(), It.IsAny<CancellationToken>())).ReturnsAsync(response);
- _favoriteRepoMock.Setup(r => r.GetFavoriteShopIdsAsync(userId, _ct)).ReturnsAsync(new HashSet<Guid> { shopId });
- _visitRepoMock.Setup(r => r.GetVisitedShopIdsAsync(userId, _ct)).ReturnsAsync(new HashSet<Guid>());
- query = BuildQuery(userId: userId);
- var result = await SearchCoffeeShopsHandler.Handle(query, _shopQueriesMock.Object, _favoriteRepoMock.Object, _visitRepoMock.Object, _cacheMock.Object, _ct);
- result.IsSuccess.Should().BeTrue();
- result.Data.CoffeeShops[0].IsFavorite.Should().BeTrue();
- result.Data.CoffeeShops[0].IsVisited.Should().BeFalse();

Тест 3: Handle_WhenCacheReturnsNull_ReturnsError
- _cacheMock.Setup(c => c.GetAsync(It.IsAny<CacheKey>(), It.IsAny<Func<CancellationToken, Task<GetCoffeeShopsResponse>>>(), It.IsAny<TimeSpan?>(), It.IsAny<CancellationToken>())).ReturnsAsync((GetCoffeeShopsResponse?)null);
- query = BuildQuery(userId: null);
- var result = await SearchCoffeeShopsHandler.Handle(query, _shopQueriesMock.Object, _favoriteRepoMock.Object, _visitRepoMock.Object, _cacheMock.Object, _ct);
- result.IsSuccess.Should().BeFalse();
- result.Message.Should().Contain("Failed to retrieve");

ЗАМЕТКА: GetFavoriteShopIdsAsync и GetVisitedShopIdsAsync возвращают IEnumerable<Guid> или HashSet<Guid> — проверить интерфейс IUserFavoriteRepository. Если возвращают IEnumerable<Guid>, то ReturnsAsync(new List<Guid> { shopId }) и Contains работает через LINQ. Если ShortShopDto.Name имеет required модификатор — убедиться что Name задан в инициализаторе.
  </action>
  <verify>
    <automated>cd /home/arseny/RiderProjects/CoffeePeek-NET && dotnet build CoffeePeek.Shops.Application.Tests --no-restore -v q 2>&1 | tail -5 && dotnet test CoffeePeek.Shops.Application.Tests --no-build --filter "SearchCoffeeShopsHandlerTests" -v n 2>&1 | tail -10</automated>
  </verify>
  <done>
    - SearchCoffeeShopsHandlerTests.cs создан с 3 тестами, все зелёные
    - ICacheService mock использует 4-параметровую перегрузку (Pitfall 1 из RESEARCH.md учтён)
  </done>
</task>

<task type="auto">
  <name>Task 2: GetCoffeeShopHandlerTests + финальный прогон</name>
  <files>
    CoffeePeek.Shops.Application.Tests/Features/CoffeeShop/GetCoffeeShopHandlerTests.cs
  </files>
  <action>
Создать файл в namespace CoffeePeek.Shops.Application.Tests.Features.CoffeeShop.GetCoffeeShop.

Явные using:
- System
- System.Threading
- System.Threading.Tasks
- CoffeePeek.Contract.Dtos.CoffeeShop (CoffeeShopDetailsDto, ReviewDto)
- CoffeePeek.Contract.Enums (PriceRange)
- CoffeePeek.Shared.Domain.Interfaces.Infrastructure (ICacheService, CacheKey)
- CoffeePeek.Shops.Application.Features.CoffeeShop.GetCoffeeShop
- CoffeePeek.Shops.Domain.Aggregates.CheckInAggregate (IQueryCheckInRepository)
- CoffeePeek.Shops.Domain.Aggregates.ReviewAggregate (IQueryReviewRepository)
- CoffeePeek.Shops.Domain.Aggregates.UserFavoriteAggregate (IUserFavoriteRepository)
- FluentAssertions
- MapsterMapper (IMapper)
- Moq

Поля класса:
```
private readonly Mock<ICoffeeShopQueries> _shopQueriesMock = new();
private readonly Mock<IUserFavoriteRepository> _favoriteRepoMock = new();
private readonly Mock<IQueryCheckInRepository> _checkInRepoMock = new();
private readonly Mock<IQueryReviewRepository> _reviewRepoMock = new();
private readonly Mock<ICacheService> _cacheMock = new();
private readonly Mock<IMapper> _mapperMock = new();
private readonly CancellationToken _ct = CancellationToken.None;
```

Вспомогательный метод BuildShopDto(Guid shopId):
- return new CoffeeShopDetailsDto { Id = shopId, Name = "Test Coffee Shop", Reviews = Array.Empty<ReviewDto>() };

Тест 1: Handle_AnonymousRequest_ReturnsShopWithoutPersonalization
- shopId = Guid.NewGuid();
- shopDto = BuildShopDto(shopId);
- _cacheMock.Setup(c => c.GetAsync(
      It.IsAny<CacheKey>(),
      It.IsAny<Func<CancellationToken, Task<CoffeeShopDetailsDto>>>(),
      It.IsAny<TimeSpan?>(),
      It.IsAny<CancellationToken>()))
    .ReturnsAsync(shopDto);
- query = new GetCoffeeShopQuery(shopId, UserId: null);
- var result = await GetCoffeeShopHandler.Handle(query, _shopQueriesMock.Object, _favoriteRepoMock.Object, _checkInRepoMock.Object, _reviewRepoMock.Object, _cacheMock.Object, _mapperMock.Object, _ct);
- result.IsSuccess.Should().BeTrue();
- result.Data.Shop.Id.Should().Be(shopId);
- _favoriteRepoMock.Verify(r => r.Exists(It.IsAny<Guid>(), It.IsAny<Guid>(), It.IsAny<CancellationToken>()), Times.Never);
- _checkInRepoMock.Verify(r => r.Exists(It.IsAny<Guid>(), It.IsAny<Guid>(), It.IsAny<CancellationToken>()), Times.Never);

Тест 2: Handle_AuthenticatedRequest_EnrichesWithFavoriteAndVisited
- shopId = Guid.NewGuid(); userId = Guid.NewGuid();
- shopDto = BuildShopDto(shopId);
- _cacheMock.Setup(c => c.GetAsync(It.IsAny<CacheKey>(), It.IsAny<Func<CancellationToken, Task<CoffeeShopDetailsDto>>>(), It.IsAny<TimeSpan?>(), It.IsAny<CancellationToken>())).ReturnsAsync(shopDto);
- _favoriteRepoMock.Setup(r => r.Exists(userId, shopId, _ct)).ReturnsAsync(true);
- _checkInRepoMock.Setup(r => r.Exists(userId, shopId, _ct)).ReturnsAsync(false);
- query = new GetCoffeeShopQuery(shopId, UserId: userId);
- var result = await GetCoffeeShopHandler.Handle(query, _shopQueriesMock.Object, _favoriteRepoMock.Object, _checkInRepoMock.Object, _reviewRepoMock.Object, _cacheMock.Object, _mapperMock.Object, _ct);
- result.IsSuccess.Should().BeTrue();
- result.Data.Shop.IsFavorite.Should().BeTrue();
- result.Data.Shop.IsVisited.Should().BeFalse();

Тест 3: Handle_WhenShopNotFound_ReturnsError
- shopId = Guid.NewGuid();
- _cacheMock.Setup(c => c.GetAsync(It.IsAny<CacheKey>(), It.IsAny<Func<CancellationToken, Task<CoffeeShopDetailsDto>>>(), It.IsAny<TimeSpan?>(), It.IsAny<CancellationToken>())).ReturnsAsync((CoffeeShopDetailsDto?)null);
- query = new GetCoffeeShopQuery(shopId, UserId: null);
- var result = await GetCoffeeShopHandler.Handle(query, _shopQueriesMock.Object, _favoriteRepoMock.Object, _checkInRepoMock.Object, _reviewRepoMock.Object, _cacheMock.Object, _mapperMock.Object, _ct);
- result.IsSuccess.Should().BeFalse();
- result.Message.Should().Contain("Shop not found");

ЗАМЕТКА по GetCoffeeShopResponse: содержит `public sealed class GetCoffeeShopResponse(CoffeeShopDetailsDto shopDto)`. Доступ к полю: result.Data.Shop (или result.Data, в зависимости от свойства). Проверить — если нет свойства Shop, обратиться к result.Data напрямую как CoffeeShopDetailsDto. Читать GetCoffeeShopResponse.cs если нужно.

ЗАМЕТКА по CoffeeShopDetailsDto: это record с `required string Name` — убедиться что Name задан в BuildShopDto. Reviews — init-only, может потребоваться явная инициализация Array.Empty<ReviewDto>().

ЗАМЕТКА по IMapper: в GetCoffeeShopHandler mapper.Map<ReviewDto[]>(reviews) вызывается внутри factory (лямбды кэша), которую мок игнорирует (ReturnsAsync возвращает готовый объект без вызова factory). Поэтому _mapperMock не нужно настраивать для этих тестов.

После создания файла выполнить финальный прогон полной тест-сюиты.
  </action>
  <verify>
    <automated>cd /home/arseny/RiderProjects/CoffeePeek-NET && dotnet test CoffeePeek.slnx 2>&1 | tail -15</automated>
  </verify>
  <done>
    - GetCoffeeShopHandlerTests.cs создан с 3 тестами, все зелёные
    - `dotnet test CoffeePeek.slnx` → 0 failed (все тест-проекты)
    - TEST-01 полностью закрыт: все 6 хэндлеров покрыты
  </done>
</task>

</tasks>

<verification>
dotnet test CoffeePeek.slnx — все тесты зелёные, 0 failed
SearchCoffeeShopsHandlerTests: 3 тестa — anonymous, authenticated (IsFavorite/IsVisited), null cache
GetCoffeeShopHandlerTests: 3 теста — anonymous, authenticated (IsFavorite/IsVisited), null cache
ICacheService mock использует перегрузку с 4 параметрами (cacheKey, factory, TimeSpan?, ct)
</verification>

<success_criteria>
- `dotnet test CoffeePeek.slnx` → 0 failed (финальный green gate для Phase 5)
- SearchCoffeeShopsHandlerTests: ICacheService mock с Pitfall-1 паттерном (4-параметровая перегрузка)
- GetCoffeeShopHandlerTests: factory в кэше игнорируется — IMapper не нужно настраивать
- TEST-01 полностью закрыт: SearchCoffeeShops, GetCoffeeShop, CreateCheckIn (план 04), AddToFavorite (план 03), GetShopsInBounds (план 03), DeleteReviewFromCoffeeShop (уже существовал)
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage/05-05-SUMMARY.md` when done
</output>
