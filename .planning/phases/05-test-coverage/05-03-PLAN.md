---
phase: 05-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - CoffeePeek.Shops.Application.Tests/Features/Favorite/AddToFavoriteHandlerTests.cs
  - CoffeePeek.Shops.Application.Tests/Features/CoffeeShop/GetShopsInBoundsHandlerTests.cs
requirements:
  - TEST-01

must_haves:
  truths:
    - "AddToFavoriteHandler вызывает AddToFavoritesAsync и SaveChangesAsync при валидной команде"
    - "AddToFavoriteHandler бросает System.ComponentModel.DataAnnotations.ValidationException при невалидной команде"
    - "GetShopsInBoundsHandler возвращает успешный ответ со списком магазинов"
    - "GetShopsInBoundsHandler возвращает успешный ответ с пустым массивом когда магазинов нет"
  artifacts:
    - path: "CoffeePeek.Shops.Application.Tests/Features/Favorite/AddToFavoriteHandlerTests.cs"
      provides: "2 теста для AddToFavoriteHandler"
    - path: "CoffeePeek.Shops.Application.Tests/Features/CoffeeShop/GetShopsInBoundsHandlerTests.cs"
      provides: "2 теста для GetShopsInBoundsHandler"
  key_links:
    - from: "AddToFavoriteHandlerTests.cs"
      to: "CoffeePeek.Shops.Application/Features/Favorite/AddToFavorite/AddToFavoriteHandler.cs"
      via: "AddToFavoriteHandler.Handle() прямой вызов"
      pattern: "AddToFavoriteHandler\\.Handle"
    - from: "GetShopsInBoundsHandlerTests.cs"
      to: "CoffeePeek.Shops.Application/Features/CoffeeShop/GetShopsInBounds/GetShopsInBoundsHandler.cs"
      via: "GetShopsInBoundsHandler.Handle() прямой вызов"
      pattern: "GetShopsInBoundsHandler\\.Handle"
---

<objective>
Покрыть двумя наиболее простыми Shops Application хэндлерами: AddToFavoriteHandler (нет кэша, синхронная валидация) и GetShopsInBoundsHandler (самый простой — один мок ICoffeeShopQueries). Тесты размещаются в уже существующем CoffeePeek.Shops.Application.Tests (GlobalUsings содержит только `global using Xunit;` — все остальные using нужны явно).

Purpose: TEST-01 частично — 2 из 6 хэндлеров.
Output: 2 новых файла с 4 тестами; `dotnet test CoffeePeek.Shops.Application.Tests` зелёный.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-test-coverage/05-RESEARCH.md

<interfaces>
<!-- Из AddToFavoriteHandler.cs -->
```csharp
using System.ComponentModel.DataAnnotations;  // ValidationException тут!
namespace CoffeePeek.Shops.Application.Features.Favorite.AddToFavorite;

public class AddToFavoriteHandler
{
    public static async Task<CreateEntityResponse<Guid>> Handle(
        AddToFavoriteCommand request,
        IUserFavoriteService userFavoriteService,
        IValidationStrategy<AddToFavoriteCommand> validationStrategy,
        IUnitOfWork unitOfWork,
        CancellationToken cancellationToken)
    // validationStrategy.Validate(request) синхронный
    // если !validationResult.IsValid → throw new ValidationException(validationResult.ErrorMessage)
    // ValidationException из System.ComponentModel.DataAnnotations (не CoffeePeek.Shared.Kernel)
    // При успехе: AddToFavoritesAsync → SaveChangesAsync → return CreateEntityResponse<Guid>.Success(id, ...)
}

public record AddToFavoriteCommand([property: JsonIgnore] Guid UserId, Guid CoffeeShopId);
```

<!-- Из GetShopsInBoundsHandler.cs -->
```csharp
namespace CoffeePeek.Shops.Application.Features.CoffeeShop.GetShopsInBounds;

public class GetShopsInBoundsHandler
{
    public static async Task<Response<GetShopsInBoundsResponse>> Handle(
        GetShopsInBoundsQuery query,
        ICoffeeShopQueries shopQueries,
        CancellationToken cancellationToken)
    // shopQueries.GetShopsInBounds(query, ct) → MapShopDto[]
    // return Response<GetShopsInBoundsResponse>.Success(new GetShopsInBoundsResponse(shops))
}

public record GetShopsInBoundsQuery(decimal MinLat, decimal MinLon, decimal MaxLat, decimal MaxLon);
public class GetShopsInBoundsResponse(IEnumerable<MapShopDto> shops) { public MapShopDto[] Shops { get; set; } = shops.ToArray(); }
```

<!-- Из DeleteReviewFromCoffeeShopHandlerTests.cs — канонический паттерн Shops.Application.Tests -->
```csharp
// Все using — явно (нет GlobalUsings кроме Xunit)
using System;
using System.Threading;
using System.Threading.Tasks;
using CoffeePeek.Shared.Kernel;          // IUnitOfWork
using CoffeePeek.Shared.Kernel.Exceptions;
using CoffeePeek.Shops.Application.Features...;
using FluentAssertions;
using Moq;
namespace CoffeePeek.Shops.Application.Tests.Features.{Feature}.{UseCase};
public class {HandlerName}Tests
{
    private readonly Mock<ISomeDep> _depMock = new();
    private readonly CancellationToken _ct = CancellationToken.None;
    [Fact]
    public async Task Handle_...()
    { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AddToFavoriteHandlerTests</name>
  <files>
    CoffeePeek.Shops.Application.Tests/Features/Favorite/AddToFavoriteHandlerTests.cs
  </files>
  <action>
Создать файл в namespace CoffeePeek.Shops.Application.Tests.Features.Favorite.AddToFavorite.

Явные using:
- System
- System.ComponentModel.DataAnnotations (для ValidationException!)
- System.Threading
- System.Threading.Tasks
- CoffeePeek.Shared.Kernel (IUnitOfWork)
- CoffeePeek.Shared.Validation (IValidationStrategy, ValidationResult)
- CoffeePeek.Shops.Application.Features.Favorite.AddToFavorite
- CoffeePeek.Shops.Domain.Aggregates.UserFavoriteAggregate (IUserFavoriteService)
- FluentAssertions
- Moq

Поля класса:
```
private readonly Mock<IUserFavoriteService> _favoriteServiceMock = new();
private readonly Mock<IValidationStrategy<AddToFavoriteCommand>> _validationMock = new();
private readonly Mock<IUnitOfWork> _unitOfWorkMock = new();
private readonly CancellationToken _ct = CancellationToken.None;
```

Тест 1: Handle_WithValidCommand_AddsToFavoritesAndSaves
- userId = Guid.NewGuid(); shopId = Guid.NewGuid();
- command = new AddToFavoriteCommand(userId, shopId);
- _validationMock.Setup(v => v.Validate(command)).Returns(Shared.Validation.ValidationResult.Valid);
- var expectedId = Guid.NewGuid();
- _favoriteServiceMock.Setup(s => s.AddToFavoritesAsync(userId, shopId, _ct)).ReturnsAsync(expectedId);
- var result = await AddToFavoriteHandler.Handle(command, _favoriteServiceMock.Object, _validationMock.Object, _unitOfWorkMock.Object, _ct);
- result.IsSuccess.Should().BeTrue();
- result.EntityId.Should().Be(expectedId);
- _favoriteServiceMock.Verify(s => s.AddToFavoritesAsync(userId, shopId, _ct), Times.Once);
- _unitOfWorkMock.Verify(u => u.SaveChangesAsync(_ct), Times.Once);

Тест 2: Handle_WithInvalidCommand_ThrowsValidationException
- command = new AddToFavoriteCommand(Guid.NewGuid(), Guid.Empty);
- _validationMock.Setup(v => v.Validate(command)).Returns(Shared.Validation.ValidationResult.Invalid("CoffeeShopId is required and cannot be empty"));
- Func<Task> act = async () => await AddToFavoriteHandler.Handle(command, _favoriteServiceMock.Object, _validationMock.Object, _unitOfWorkMock.Object, _ct);
- await act.Should().ThrowAsync<ValidationException>();
- _favoriteServiceMock.Verify(s => s.AddToFavoritesAsync(It.IsAny<Guid>(), It.IsAny<Guid>(), It.IsAny<CancellationToken>()), Times.Never);
- _unitOfWorkMock.Verify(u => u.SaveChangesAsync(It.IsAny<CancellationToken>()), Times.Never);

ВАЖНО: ValidationException здесь — System.ComponentModel.DataAnnotations.ValidationException (из хэндлера). Не путать с CoffeePeek.Shared.Kernel.Exceptions.ValidationException. В AddToFavoriteHandler.cs используется `throw new ValidationException(...)` из System.ComponentModel.DataAnnotations.
  </action>
  <verify>
    <automated>cd /home/arseny/RiderProjects/CoffeePeek-NET && dotnet build CoffeePeek.Shops.Application.Tests --no-restore -v q 2>&1 | tail -5 && dotnet test CoffeePeek.Shops.Application.Tests --no-build --filter "AddToFavoriteHandlerTests" -v n 2>&1 | tail -10</automated>
  </verify>
  <done>
    - AddToFavoriteHandlerTests.cs создан с 2 тестами, оба зелёные
    - Нет ошибок компиляции
  </done>
</task>

<task type="auto">
  <name>Task 2: GetShopsInBoundsHandlerTests</name>
  <files>
    CoffeePeek.Shops.Application.Tests/Features/CoffeeShop/GetShopsInBoundsHandlerTests.cs
  </files>
  <action>
Создать файл в namespace CoffeePeek.Shops.Application.Tests.Features.CoffeeShop.GetShopsInBounds.

Явные using:
- System
- System.Threading
- System.Threading.Tasks
- CoffeePeek.Contract.Dtos.CoffeeShop (MapShopDto)
- CoffeePeek.Shops.Application.Features.CoffeeShop.GetCoffeeShop (ICoffeeShopQueries)
- CoffeePeek.Shops.Application.Features.CoffeeShop.GetShopsInBounds
- FluentAssertions
- Moq

Поля класса:
```
private readonly Mock<ICoffeeShopQueries> _shopQueriesMock = new();
private readonly CancellationToken _ct = CancellationToken.None;
```

Тест 1: Handle_WithValidQuery_ReturnsSuccessWithShops
- var shopId = Guid.NewGuid();
- var shops = new MapShopDto[] { new MapShopDto { Id = shopId } };
- query = new GetShopsInBoundsQuery(55.0m, 37.0m, 55.5m, 37.5m);
- _shopQueriesMock.Setup(q => q.GetShopsInBounds(query, _ct)).ReturnsAsync(shops);
- var result = await GetShopsInBoundsHandler.Handle(query, _shopQueriesMock.Object, _ct);
- result.IsSuccess.Should().BeTrue();
- result.Data.Shops.Should().HaveCount(1);
- result.Data.Shops[0].Id.Should().Be(shopId);

Тест 2: Handle_WithNoShopsInBounds_ReturnsSuccessWithEmptyArray
- query = new GetShopsInBoundsQuery(0m, 0m, 0.1m, 0.1m);
- _shopQueriesMock.Setup(q => q.GetShopsInBounds(query, _ct)).ReturnsAsync(Array.Empty<MapShopDto>());
- var result = await GetShopsInBoundsHandler.Handle(query, _shopQueriesMock.Object, _ct);
- result.IsSuccess.Should().BeTrue();
- result.Data.Shops.Should().BeEmpty();

ЗАМЕТКА по MapShopDto: проверить конструктор/инициализатор — если у MapShopDto нет публичного конструктора с параметрами или required-свойств кроме Id, использовать object initializer new MapShopDto { Id = shopId }. Если MapShopDto — record, использовать соответственно. Главное: поле Id должно быть доступно.
  </action>
  <verify>
    <automated>cd /home/arseny/RiderProjects/CoffeePeek-NET && dotnet test CoffeePeek.Shops.Application.Tests 2>&1 | tail -10</automated>
  </verify>
  <done>
    - GetShopsInBoundsHandlerTests.cs создан с 2 тестами, оба зелёные
    - Все 9 тестов CoffeePeek.Shops.Application.Tests зелёные (5 existing + 4 new)
  </done>
</task>

</tasks>

<verification>
dotnet test CoffeePeek.Shops.Application.Tests — 9 passed (5 existing + 4 new), 0 failed
ValidationException в AddToFavoriteHandlerTests — System.ComponentModel.DataAnnotations, не Shared.Kernel
</verification>

<success_criteria>
- `dotnet test CoffeePeek.Shops.Application.Tests` → 0 failed
- AddToFavoriteHandlerTests: валидный путь проверяет AddToFavoritesAsync+SaveChangesAsync; невалидный путь бросает System.ComponentModel.DataAnnotations.ValidationException
- GetShopsInBoundsHandlerTests: список магазинов и пустой список оба возвращают IsSuccess=true
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage/05-03-SUMMARY.md` when done
</output>
