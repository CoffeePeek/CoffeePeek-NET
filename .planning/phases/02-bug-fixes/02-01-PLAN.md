---
phase: 02-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CoffeePeek.Shared.Domain/Interfaces/Infrastructure/CacheKey.cs
  - CoffeePeek.Shops.Application/Features/CoffeeShop/SearchCoffeeShops/SearchCoffeeShopsHandler.cs
  - CoffeePeek.ShopsService/Controllers/CoffeeShopsController.cs
  - CoffeePeek.Shops.Domain.Tests/CacheKey/CoffeeBeanCacheKeyTests.cs
autonomous: true
requirements: [BUG-01, BUG-02, BUG-04]
tags: [bug-fix, cache, error-handling, personalization]

must_haves:
  truths:
    - "CacheKey.CoffeeBean.ListPattern() returns a string whose prefix matches CacheKey.CoffeeBean.ListAll().Key prefix"
    - "SearchCoffeeShopsHandler returns a descriptive error message (not empty 'Error') when the cached response is null"
    - "Authenticated users see correct IsFavorite/IsVisited values when calling GET /api/CoffeeShops/{id}"
  artifacts:
    - path: "CoffeePeek.Shared.Domain/Interfaces/Infrastructure/CacheKey.cs"
      provides: "Fixed CoffeeBean.ListPattern() returning 'coffeebean:list:*'"
      contains: 'return "coffeebean:list:*"'
    - path: "CoffeePeek.Shops.Application/Features/CoffeeShop/SearchCoffeeShops/SearchCoffeeShopsHandler.cs"
      provides: "Descriptive error message on null cached response"
      contains: "Failed to retrieve coffee shop search results"
    - path: "CoffeePeek.ShopsService/Controllers/CoffeeShopsController.cs"
      provides: "GetCoffeeShop passes userId to GetCoffeeShopQuery"
      contains: "userContext.GetUserId()"
    - path: "CoffeePeek.Shops.Domain.Tests/CacheKey/CoffeeBeanCacheKeyTests.cs"
      provides: "Regression test: CoffeeBean.ListPattern() prefix matches ListAll().Key prefix"
      contains: "CoffeeBean.ListPattern()"
  key_links:
    - from: "CoffeePeek.Shared.Domain.CacheKey.CoffeeBean.ListPattern()"
      to: "Categories.Shops.Patterns dictionary entry for 'dictionaries'"
      via: "Direct string match prefix with ListAll().Key"
      pattern: '"coffeebean:list:\*"'
    - from: "CoffeeShopsController.GetCoffeeShop (line ~97)"
      to: "GetCoffeeShopQuery constructor"
      via: "userContext.GetUserId() passed as second argument"
      pattern: 'new GetCoffeeShopQuery\\(id, userContext\\.GetUserId\\(\\)\\)'
---

<objective>
Apply three surgical, low-risk single-line fixes plus a BUG-01 regression test:
1. BUG-01: Align CoffeeBean cache invalidation pattern with the write key prefix.
2. BUG-02: Replace meaningless "Error" string with a descriptive error message in SearchCoffeeShops handler.
3. BUG-04: Thread the authenticated user id into GetCoffeeShop so IsFavorite/IsVisited are populated correctly.
4. BUG-01 regression: Add a [Fact] to CoffeePeek.Shops.Domain.Tests asserting ListPattern() shares the "coffeebean" prefix with ListAll().Key.

Purpose: Restore correctness of cache invalidation, error reporting, and per-user personalization. Lock in BUG-01 with a test so the prefix mismatch cannot regress silently.
Output: Three modified source files + one new test file. All other fixes for Phase 2 (BUG-03, BUG-05) are handled in separate plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bug-fixes/02-RESEARCH.md
@.planning/phases/02-bug-fixes/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@CoffeePeek.Shared.Domain/Interfaces/Infrastructure/CacheKey.cs
@CoffeePeek.Shops.Application/Features/CoffeeShop/SearchCoffeeShops/SearchCoffeeShopsHandler.cs
@CoffeePeek.ShopsService/Controllers/CoffeeShopsController.cs

<interfaces>
<!-- Key signatures executors will rely on. Extracted directly from current sources. -->

From CoffeePeek.Shared.Domain/Interfaces/Infrastructure/CacheKey.cs (lines 126-135):
- Nested static class `CoffeeBean` declares two methods:
  - `public static CacheKey ListAll()` returns `new(Key: "coffeebean:list:all", ...)`
  - `public static string ListPattern()` currently returns `"bean:list:*"` (the bug)
- Convention used by every other catalog (City, Equipment, Roaster, BrewMethod):
  - `ListAll().Key` = `"{prefix}:list:all"`, `ListPattern()` = `"{prefix}:list:*"`

From CoffeePeek.Shops.Application/Features/CoffeeShop/SearchCoffeeShops/SearchCoffeeShopsHandler.cs:
- `public static async Task<Response<GetCoffeeShopsResponse>> Handle(SearchCoffeeShopsQuery queryRequest, ICoffeeShopQueries coffeeShopQueries, IUserFavoriteRepository favoriteRepository, IQueryCheckInRepository visitRepository, ICacheService redisService, CancellationToken ct)`
- Line 37 currently: `if (cachedResponse == null) return Response<GetCoffeeShopsResponse>.Error("Error");`

From CoffeePeek.ShopsService/Controllers/CoffeeShopsController.cs:
- Constructor: `CoffeeShopsController(IMessageBus bus, IUserContext userContext)`
- `GetCoffeeShops` (line 49) already uses `UserId: userContext.GetUserId()` (the reference pattern)
- `GetCoffeeShop(Guid id)` action at line 95 currently constructs the query as `new GetCoffeeShopQuery(id)` on line 97
- `GetCoffeeShopQuery` is declared as `public record GetCoffeeShopQuery(Guid Id, Guid? UserId = null)` — second positional parameter is optional `Guid?`
- `IUserContext.GetUserId()` returns `Guid?` (null when anonymous); `GetUserIdOrThrow()` returns non-null `Guid` and is used only on `[Authorize]` endpoints
- `GetCoffeeShop` is NOT decorated with `[Authorize]`, so `GetUserId()` is the correct accessor

From CoffeePeek.Shops.Application/Features/CoffeeShop/GetCoffeeShop/GetCoffeeShopHandler.cs:
- Handler already conditionally fetches `IsFavorite` / `IsVisited` only when `query.UserId.HasValue` (lines 43-50). No handler change is needed for BUG-04 — the bug is purely in the controller.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix BUG-01 — align CoffeeBean ListPattern prefix with write key</name>
  <files>CoffeePeek.Shared.Domain/Interfaces/Infrastructure/CacheKey.cs</files>
  <read_first>
    - CoffeePeek.Shared.Domain/Interfaces/Infrastructure/CacheKey.cs (entire file — see how City lines 106-113, Equipment lines 116-124, Roaster lines 137-146, and BrewMethod lines 148-157 follow the matching prefix convention; the CoffeeBean nested class at lines 126-135 is the only outlier)
    - .planning/phases/02-bug-fixes/02-PATTERNS.md (section: CacheKey.cs)
  </read_first>
  <action>
    In the nested static class `CacheKey.CoffeeBean` (around line 126), change the `ListPattern()` method so it returns the literal string `"coffeebean:list:*"` instead of `"bean:list:*"`. The write key on line 129 is `"coffeebean:list:all"`; the pattern must share the same `"coffeebean:"` prefix so Redis pattern-based invalidation actually matches it. Do not touch any other nested class, any other method, or the `Categories.Shops.Patterns` dictionary (it transitively picks up the new value via `CoffeeBean.ListPattern()`). No `using` directives need to change. Single-line edit per BUG-01.
  </action>
  <verify>
    <automated>grep -nE 'ListPattern\(\)\s*=>\s*"coffeebean:list:\*"' CoffeePeek.Shared.Domain/Interfaces/Infrastructure/CacheKey.cs &amp;&amp; ! grep -nE 'ListPattern\(\)\s*=>\s*"bean:list:\*"' CoffeePeek.Shared.Domain/Interfaces/Infrastructure/CacheKey.cs &amp;&amp; dotnet build CoffeePeek.Shared.Domain/CoffeePeek.Shared.Domain.csproj -c Debug --nologo --verbosity quiet</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `CoffeePeek.Shared.Domain/Interfaces/Infrastructure/CacheKey.cs` contains the exact substring `ListPattern() => "coffeebean:list:*"` inside the `CoffeeBean` nested class (use grep with the pattern above).
    - source-assertion: `CoffeePeek.Shared.Domain/Interfaces/Infrastructure/CacheKey.cs` does NOT contain the substring `"bean:list:*"` anywhere.
    - source-assertion: `CoffeePeek.Shared.Domain/Interfaces/Infrastructure/CacheKey.cs` still contains the substring `Key: "coffeebean:list:all"` (write key untouched).
    - cli: `dotnet build CoffeePeek.Shared.Domain/CoffeePeek.Shared.Domain.csproj -c Debug --nologo --verbosity quiet` exits 0.
  </acceptance_criteria>
  <done>CoffeeBean.ListPattern() returns "coffeebean:list:*" and the Shared.Domain project builds with no warnings or errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix BUG-02 — descriptive error message in SearchCoffeeShopsHandler</name>
  <files>CoffeePeek.Shops.Application/Features/CoffeeShop/SearchCoffeeShops/SearchCoffeeShopsHandler.cs</files>
  <read_first>
    - CoffeePeek.Shops.Application/Features/CoffeeShop/SearchCoffeeShops/SearchCoffeeShopsHandler.cs (entire file — note that line 37 is the only `Response.Error(...)` call; line 52 returns the success path)
    - .planning/phases/02-bug-fixes/02-PATTERNS.md (section: SearchCoffeeShopsHandler.cs)
  </read_first>
  <action>
    On line 37 of `SearchCoffeeShopsHandler.cs`, change the call `Response&lt;GetCoffeeShopsResponse&gt;.Error("Error")` so the error message is the exact string `"Failed to retrieve coffee shop search results"` instead of `"Error"`. Do not modify the surrounding null check, do not touch the `CreateSearchHash` / `ComputeHash` helpers, do not change any other line. Imports stay unchanged.
  </action>
  <verify>
    <automated>grep -nE 'Response&lt;GetCoffeeShopsResponse&gt;\.Error\("Failed to retrieve coffee shop search results"\)' CoffeePeek.Shops.Application/Features/CoffeeShop/SearchCoffeeShops/SearchCoffeeShopsHandler.cs &amp;&amp; ! grep -nE 'Response&lt;GetCoffeeShopsResponse&gt;\.Error\("Error"\)' CoffeePeek.Shops.Application/Features/CoffeeShop/SearchCoffeeShops/SearchCoffeeShopsHandler.cs &amp;&amp; dotnet build CoffeePeek.Shops.Application/CoffeePeek.Shops.Application.csproj -c Debug --nologo --verbosity quiet</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `SearchCoffeeShopsHandler.cs` contains the exact substring `Response<GetCoffeeShopsResponse>.Error("Failed to retrieve coffee shop search results")`.
    - source-assertion: `SearchCoffeeShopsHandler.cs` does NOT contain the substring `Response<GetCoffeeShopsResponse>.Error("Error")`.
    - source-assertion: `SearchCoffeeShopsHandler.cs` still contains the null guard `if (cachedResponse == null)` on the same line as the error return.
    - cli: `dotnet build CoffeePeek.Shops.Application/CoffeePeek.Shops.Application.csproj -c Debug --nologo --verbosity quiet` exits 0.
  </acceptance_criteria>
  <done>The null-response branch in SearchCoffeeShopsHandler returns a descriptive error message and the Shops.Application project builds clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix BUG-04 — pass userContext.GetUserId() into GetCoffeeShopQuery</name>
  <files>CoffeePeek.ShopsService/Controllers/CoffeeShopsController.cs</files>
  <read_first>
    - CoffeePeek.ShopsService/Controllers/CoffeeShopsController.cs (entire file — line 49 already uses `UserId: userContext.GetUserId()` in `GetCoffeeShops`; mirror that pattern in `GetCoffeeShop` on line 97. The action is NOT decorated with `[Authorize]`.)
    - CoffeePeek.Shops.Application/Features/CoffeeShop/GetCoffeeShop/GetCoffeeShopQuery.cs (confirms second parameter is `Guid? UserId = null`)
    - .planning/phases/02-bug-fixes/02-PATTERNS.md (section: CoffeeShopsController.cs)
  </read_first>
  <action>
    In `CoffeeShopsController.GetCoffeeShop(Guid id)` (line 95-101), replace the query construction `new GetCoffeeShopQuery(id)` (line 97) with `new GetCoffeeShopQuery(id, userContext.GetUserId())`. Use `GetUserId()` (returns `Guid?`), not `GetUserIdOrThrow()`, because the action is anonymous-friendly (no `[Authorize]`). Do not introduce additional null checks — `GetCoffeeShopQuery` already accepts a nullable `UserId` and `GetCoffeeShopHandler` lines 43-50 handle both the present and absent cases. Do not change route, attributes, return type, or any other action in the controller. No new `using` directives required (the `IUserContext` and `GetCoffeeShopQuery` namespaces are already imported on lines 2 and 5).
  </action>
  <verify>
    <automated>grep -nE 'new GetCoffeeShopQuery\(id, userContext\.GetUserId\(\)\)' CoffeePeek.ShopsService/Controllers/CoffeeShopsController.cs &amp;&amp; ! grep -nE 'new GetCoffeeShopQuery\(id\)\s*;' CoffeePeek.ShopsService/Controllers/CoffeeShopsController.cs &amp;&amp; dotnet build CoffeePeek.ShopsService/CoffeePeek.ShopsService.csproj -c Debug --nologo --verbosity quiet</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `CoffeeShopsController.cs` contains the exact substring `new GetCoffeeShopQuery(id, userContext.GetUserId())`.
    - source-assertion: `CoffeeShopsController.cs` does NOT contain the substring `new GetCoffeeShopQuery(id);` (single-argument constructor call must be gone).
    - source-assertion: The `GetCoffeeShop` action still lacks an `[Authorize]` attribute (no `[Authorize]` line appears between the `[HttpGet("{id:guid}")]` attribute and the `public async Task<IActionResult> GetCoffeeShop(Guid id)` signature).
    - cli: `dotnet build CoffeePeek.ShopsService/CoffeePeek.ShopsService.csproj -c Debug --nologo --verbosity quiet` exits 0.
  </acceptance_criteria>
  <done>GET /api/CoffeeShops/{id} forwards the authenticated user's id (or null when anonymous) so IsFavorite and IsVisited are populated; ShopsService project builds without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Regression test BUG-01 — assert CoffeeBean.ListPattern() prefix matches ListAll().Key</name>
  <files>CoffeePeek.Shops.Domain.Tests/CacheKey/CoffeeBeanCacheKeyTests.cs</files>
  <read_first>
    - CoffeePeek.Shops.Domain.Tests/CoffeePeek.Shops.Domain.Tests.csproj (confirms the project exists and has a ProjectReference to Shared.Domain; no new csproj changes needed)
    - CoffeePeek.Shared.Domain/Interfaces/Infrastructure/CacheKey.cs (after Task 1's fix — confirms CoffeeBean.ListAll().Key = "coffeebean:list:all" and CoffeeBean.ListPattern() = "coffeebean:list:*")
  </read_first>
  <behavior>
    - `CacheKey.CoffeeBean.ListPattern()` starts with the same leading token as `CacheKey.CoffeeBean.ListAll().Key` — specifically, both begin with `"coffeebean"`.
    - The pattern string ends with `"*"` (wildcard) confirming it is a Redis pattern, not a literal key.
  </behavior>
  <action>
    Create directory `CoffeePeek.Shops.Domain.Tests/CacheKey/` and file `CoffeeBeanCacheKeyTests.cs` inside it. Use namespace `CoffeePeek.Shops.Domain.Tests.CacheKey`. Add `using CoffeePeek.Shared.Domain.Interfaces.Infrastructure;` (the namespace containing `CacheKey`) and `using FluentAssertions;` (already a test-project dependency). The test project is `CoffeePeek.Shops.Domain.Tests` which already exists and has a reference to `CoffeePeek.Shared.Domain` — no csproj changes are required.

    Write a single `public class CoffeeBeanCacheKeyTests` with one `[Fact]` named `ListPattern_StartsWithSamePrefixAsListAllKey`. The test body: get the `allKey` via `CacheKey.CoffeeBean.ListAll().Key` and the `pattern` via `CacheKey.CoffeeBean.ListPattern()`. Extract the prefix from `allKey` as the substring up to (not including) the second colon (i.e., `allKey.Split(':')[0]`). Assert that `pattern.Should().StartWith(prefix)` and `pattern.Should().EndWith("*")`. This single assertion locks in the BUG-01 fix permanently: if the prefix ever drifts again, the test will catch it.

    Do not add any mocks, repositories, or additional test classes. The assertion is a pure string property test — no service container, no database, no I/O.
  </action>
  <verify>
    <automated>test -f CoffeePeek.Shops.Domain.Tests/CacheKey/CoffeeBeanCacheKeyTests.cs &amp;&amp; grep -q 'ListPattern_StartsWithSamePrefixAsListAllKey' CoffeePeek.Shops.Domain.Tests/CacheKey/CoffeeBeanCacheKeyTests.cs &amp;&amp; dotnet test CoffeePeek.Shops.Domain.Tests/CoffeePeek.Shops.Domain.Tests.csproj --filter "FullyQualifiedName~CoffeeBeanCacheKeyTests" --nologo --verbosity quiet</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `CoffeePeek.Shops.Domain.Tests/CacheKey/CoffeeBeanCacheKeyTests.cs` exists.
    - source-assertion: The file contains one `[Fact]` method named `ListPattern_StartsWithSamePrefixAsListAllKey`.
    - source-assertion: The file references both `CacheKey.CoffeeBean.ListAll()` and `CacheKey.CoffeeBean.ListPattern()`.
    - test-command: `dotnet test CoffeePeek.Shops.Domain.Tests/CoffeePeek.Shops.Domain.Tests.csproj --filter "FullyQualifiedName~CoffeeBeanCacheKeyTests" --nologo --verbosity quiet` exits 0 with 1 test passed.
  </acceptance_criteria>
  <done>The regression test for BUG-01 passes, ensuring CoffeeBean.ListPattern() will always share the "coffeebean" prefix with ListAll().Key. The Shops.Domain.Tests project builds and the new Fact is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Gateway | Untrusted HTTP request reaches `GetCoffeeShop` / `GetCoffeeShops` / dictionary cache endpoints. JWT validated at the Gateway when present; action allows anonymous. |
| Application -> Redis | Cache keys and patterns are constructed in Shared.Domain; mismatched prefixes silently no-op pattern operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | `GetCoffeeShop` returning shared cached `CoffeeShopDetailsDto` | mitigate | Per RESEARCH.md, `IsFavorite`/`IsVisited` are added AFTER cache retrieval inside the handler; passing `userContext.GetUserId()` does not bake user data into the shared cache key. Verified in `GetCoffeeShopHandler` lines 43-50. |
| T-02-02 | Tampering | Stale CoffeeBean catalog entries when admin invalidates `Categories.Shops.Patterns["dictionaries"]` | mitigate | BUG-01 fix re-aligns `CoffeeBean.ListPattern()` with the write key prefix so SCAN/KEYS-based invalidation actually clears the entry. Without the fix, admins believe the cache is cleared while clients keep reading stale data. |
| T-02-03 | Repudiation / Denial of information | Empty `"Error"` string in `SearchCoffeeShopsHandler` | accept | Low risk on its own; BUG-02 fix replaces with a descriptive message so logs and clients can attribute the failure. |
| T-02-SC | Supply chain (npm/pip/cargo) | n/a | accept | No package installs in this plan. RESEARCH.md `## Package Legitimacy Audit` documents "no external packages installed in this phase." |
</threat_model>

<verification>
After all three tasks succeed, run the full solution build and the existing test suite as a regression gate:

```bash
dotnet build CoffeePeek.slnx -c Debug --nologo --verbosity quiet
dotnet test CoffeePeek.slnx --nologo --verbosity quiet
```

Both commands must exit with code 0. The existing Account / Shops domain tests must continue to pass with no new red tests introduced.
</verification>

<success_criteria>
- `CacheKey.CoffeeBean.ListPattern()` returns `"coffeebean:list:*"` so it matches the write key prefix.
- `SearchCoffeeShopsHandler` returns the message `"Failed to retrieve coffee shop search results"` on null cached response.
- `CoffeeShopsController.GetCoffeeShop` constructs `new GetCoffeeShopQuery(id, userContext.GetUserId())`.
- Solution builds and existing test suite stays green.
- BUG-01, BUG-02, BUG-04 marked complete in ROADMAP/REQUIREMENTS during phase rollup.
</success_criteria>

<output>
Create `.planning/phases/02-bug-fixes/02-01-SUMMARY.md` when done, listing the three fixes applied, the exact lines touched, and the verification commands run.
</output>
