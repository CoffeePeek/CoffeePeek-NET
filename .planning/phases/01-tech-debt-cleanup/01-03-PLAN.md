---
phase: 01-tech-debt-cleanup
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - CoffeePeek.Shared.Persistence/Extensions/WolverineModule.cs
  - CoffeePeek.Shared.Web/Extensions/CorsModule.cs
  - CoffeePeek.Shared.Web/Handlers/GlobalExceptionHandler.cs
autonomous: true
requirements:
  - TD-07

must_haves:
  truths:
    - "WolverineModule TypeLoadMode is controlled by IHostEnvironment.IsDevelopment() at runtime, not #if DEBUG"
    - "CorsModule allows localhost/LAN origins when IsDevelopment() is true, requires ALLOWED_ORIGINS env var in production"
    - "GlobalExceptionHandler includes StackTrace/InnerException detail when IsDevelopment() is true"
    - "No #if DEBUG blocks remain in any of the three target files"
  artifacts:
    - path: "CoffeePeek.Shared.Persistence/Extensions/WolverineModule.cs"
      provides: "Wolverine config with runtime environment check"
      contains: "IsDevelopment"
    - path: "CoffeePeek.Shared.Web/Extensions/CorsModule.cs"
      provides: "CORS config with runtime environment check"
      contains: "IsDevelopment"
    - path: "CoffeePeek.Shared.Web/Handlers/GlobalExceptionHandler.cs"
      provides: "Exception handler with runtime environment check"
      contains: "IsDevelopment"
  key_links:
    - from: "CorsModule.cs"
      to: "IWebHostEnvironment"
      via: "IsDevelopment() replaces #if DEBUG"
      pattern: "IsDevelopment"
---

<objective>
Replace all three #if DEBUG compile-time blocks in shared libraries with runtime IHostEnvironment / IWebHostEnvironment checks. This allows the same compiled binary to behave correctly in both development (Aspire-run) and production (Release build) modes without requiring a debug build for local development.

Purpose: A Release build must work in development (Aspire) without special environment variables, and must enforce production CORS/error-detail restrictions. The fix must not change visible behavior in either environment.
Output: Modified WolverineModule.cs, CorsModule.cs, GlobalExceptionHandler.cs — all three lose their #if DEBUG blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
</context>

<interfaces>
From current file state:

WolverineModule.cs — extension method on WebApplicationBuilder:
  extension(WebApplicationBuilder builder)
  {
      public void AddWolverine(Assembly[] handlerAssembly)
  }
  The builder has builder.Environment (IHostEnvironment) available.
  Current #if DEBUG block: opts.CodeGeneration.TypeLoadMode = TypeLoadMode.Auto;
  Runtime replacement: if (builder.Environment.IsDevelopment()) opts.CodeGeneration.TypeLoadMode = TypeLoadMode.Auto;

CorsModule.cs — static extension on IServiceCollection:
  public static IServiceCollection AddCorsModule(this IServiceCollection services)
  Currently takes no IHostEnvironment parameter.
  Must change signature to: AddCorsModule(this IServiceCollection services, IHostEnvironment environment)
  All callers (typically in each service's DependencyInjection.cs) pass builder.Environment.
  Runtime replacement: replace #if DEBUG / #else / #endif with if (environment.IsDevelopment()) / else.

GlobalExceptionHandler.cs — already has IWebHostEnvironment injected via constructor:
  public class GlobalExceptionHandler(ILogger<GlobalExceptionHandler> logger, IWebHostEnvironment environment)
  GetSafeMessage already uses environment.IsDevelopment().
  The #if DEBUG block in TryHandleAsync: replace with if (environment.IsDevelopment()) / else for errorResponse construction.
  No signature changes needed here.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace #if DEBUG in WolverineModule and GlobalExceptionHandler (TD-07)</name>
  <files>
    CoffeePeek.Shared.Persistence/Extensions/WolverineModule.cs
    CoffeePeek.Shared.Web/Handlers/GlobalExceptionHandler.cs
  </files>
  <read_first>
    - CoffeePeek.Shared.Persistence/Extensions/WolverineModule.cs (full file)
    - CoffeePeek.Shared.Web/Handlers/GlobalExceptionHandler.cs (full file)
  </read_first>
  <action>
    WolverineModule.cs:
    Line 24 contains `#if DEBUG` / `#endif` wrapping `opts.CodeGeneration.TypeLoadMode = TypeLoadMode.Auto;`. Replace with a runtime check: `if (builder.Environment.IsDevelopment()) { opts.CodeGeneration.TypeLoadMode = TypeLoadMode.Auto; }`. The builder variable is accessible because this is an extension on WebApplicationBuilder. No using changes needed — IHostEnvironment is already referenced via Microsoft.Extensions.Hosting (already imported for other uses or add it).

    GlobalExceptionHandler.cs:
    Lines 28-36 contain the #if DEBUG / #else / #endif block building errorResponse. Replace with:
      ErrorResponse errorResponse;
      if (environment.IsDevelopment())
      {
          errorResponse = new ErrorResponse(GetSafeMessage(exception))
          {
              StackTrace = exception.StackTrace,
              InnerException = exception.InnerException?.Message
          };
      }
      else
      {
          errorResponse = new ErrorResponse(GetSafeMessage(exception));
      }
    The environment field already exists in the constructor. No using changes needed — IWebHostEnvironment is already imported.

    Remove all #if / #else / #endif preprocessor directives from both files.
  </action>
  <verify>
    <automated>grep -n "#if\|#else\|#endif" CoffeePeek.Shared.Persistence/Extensions/WolverineModule.cs CoffeePeek.Shared.Web/Handlers/GlobalExceptionHandler.cs</automated>
  </verify>
  <done>No preprocessor directives in either file. Both files compile. IsDevelopment() check replaces #if DEBUG in each. dotnet build CoffeePeek.slnx succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Replace #if DEBUG in CorsModule and update all callers (TD-07)</name>
  <files>
    CoffeePeek.Shared.Web/Extensions/CorsModule.cs
  </files>
  <read_first>
    - CoffeePeek.Shared.Web/Extensions/CorsModule.cs (full file)
    - Search callers: grep -rn "AddCorsModule" across the solution to find all call sites
  </read_first>
  <action>
    CorsModule.cs currently has no environment parameter. The #if DEBUG / #else / #endif block controls whether localhost origins or ALLOWED_ORIGINS env var is used.

    Step 1 — Update method signature:
    Change `public static IServiceCollection AddCorsModule(this IServiceCollection services)` to `public static IServiceCollection AddCorsModule(this IServiceCollection services, IHostEnvironment environment)`. Add `using Microsoft.Extensions.Hosting;` if not already present.

    Step 2 — Replace compile-time branch with runtime check:
    Inside AddCors, replace the #if DEBUG / #else / #endif block with:
      if (environment.IsDevelopment())
      {
          policy
              .SetIsOriginAllowed(origin =>
              {
                  var host = new Uri(origin).Host;
                  return host == "localhost" ||
                         host == "127.0.0.1" ||
                         host.StartsWith("192.168.") ||
                         host.StartsWith("10.") ||
                         host.EndsWith(".local");
              })
              .AllowAnyHeader()
              .AllowAnyMethod()
              .AllowCredentials();
      }
      else
      {
          if (origins.Length == 0)
              throw new InvalidOperationException("CORS enabled but ALLOWED_ORIGINS is empty");

          policy
              .WithOrigins(origins)
              .AllowAnyHeader()
              .AllowAnyMethod()
              .AllowCredentials();
      }

    Step 3 — Update all callers:
    Run `grep -rn "AddCorsModule" --include="*.cs"` to find every call site. For each caller, add `builder.Environment` (or `app.Environment` / `webHostEnvironment` depending on context) as the second argument. Typically called in a service's DependencyInjection.cs or Program.cs where WebApplicationBuilder is available — pass `builder.Environment`.
  </action>
  <verify>
    <automated>grep -n "#if\|#else\|#endif" CoffeePeek.Shared.Web/Extensions/CorsModule.cs</automated>
  </verify>
  <done>No preprocessor directives in CorsModule.cs. Method accepts IHostEnvironment parameter. All callers pass environment. dotnet build CoffeePeek.slnx succeeds with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev environment→CORS policy | IsDevelopment() gate must be trustworthy — set via ASPNETCORE_ENVIRONMENT env var, not untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | CorsModule dev origin allowlist | mitigate | IsDevelopment() is controlled by ASPNETCORE_ENVIRONMENT; production deployments must not set Development environment — document in deployment runbook |
| T-03-02 | Information Disclosure | GlobalExceptionHandler stack trace | mitigate | Stack trace only emitted when IsDevelopment() is true — same behavior as before, now runtime-controlled |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks:
1. `grep -rn "#if\|#else\|#endif" CoffeePeek.Shared.Persistence/Extensions/WolverineModule.cs CoffeePeek.Shared.Web/Extensions/CorsModule.cs CoffeePeek.Shared.Web/Handlers/GlobalExceptionHandler.cs` — no output
2. `dotnet build CoffeePeek.slnx` — zero errors
3. `grep -n "IsDevelopment" CoffeePeek.Shared.Web/Extensions/CorsModule.cs CoffeePeek.Shared.Persistence/Extensions/WolverineModule.cs CoffeePeek.Shared.Web/Handlers/GlobalExceptionHandler.cs` — each file has at least one match
</verification>

<success_criteria>
- TD-07: All three shared library files contain zero #if/#else/#endif preprocessor directives
- Runtime behavior identical to prior compile-time behavior in both development (IsDevelopment=true) and production (IsDevelopment=false)
- dotnet build CoffeePeek.slnx passes
</success_criteria>

<output>
Create `.planning/phases/01-tech-debt-cleanup/01-03-SUMMARY.md` when done
</output>
