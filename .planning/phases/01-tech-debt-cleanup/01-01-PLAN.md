---
phase: 01-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CoffeePeek.Shops.Application/Services/CreateShopFromModerationService.cs
  - CoffeePeek.Shops.Application/Services/UserFavoriteService.cs
  - CoffeePeek.Shops.Infrastructure/Services/CoffeeShopRepository.cs
autonomous: true
requirements:
  - TD-02
  - TD-04
  - TD-06

must_haves:
  truths:
    - "CoffeeShop constructor call is not immediately followed by a redundant UpdateDetails call with the same values"
    - "UserFavoriteService throws ValidationException (not InvalidOperationException) for empty Guid guards"
    - "CoffeeShopRepository.cs no longer exists as a near-empty file"
  artifacts:
    - path: "CoffeePeek.Shops.Application/Services/CreateShopFromModerationService.cs"
      provides: "Shop creation service without redundant UpdateDetails call"
      contains: "new CoffeeShop("
    - path: "CoffeePeek.Shops.Application/Services/UserFavoriteService.cs"
      provides: "Favorite service with ValidationException guards"
      contains: "throw new ValidationException"
  key_links:
    - from: "UserFavoriteService.cs"
      to: "GlobalExceptionHandler"
      via: "ValidationException maps to HTTP 400"
      pattern: "ValidationException"
---

<objective>
Fix three independent Shops-layer tech debt items: remove a redundant constructor-duplicating UpdateDetails call, replace InvalidOperationException guards with ValidationException so clients receive HTTP 400 instead of 500, and delete the effectively empty CoffeeShopRepository.cs file.

Purpose: Correct silent bugs (wrong HTTP status code, misleading code duplication) with zero functional change to business logic.
Output: Modified CreateShopFromModerationService.cs, modified UserFavoriteService.cs, deleted CoffeeShopRepository.cs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant UpdateDetails call (TD-02)</name>
  <files>CoffeePeek.Shops.Application/Services/CreateShopFromModerationService.cs</files>
  <read_first>
    - CoffeePeek.Shops.Application/Services/CreateShopFromModerationService.cs (read the full file — focus on lines 25-26)
  </read_first>
  <action>
    In CreateShopFromModerationService.cs, line 26 calls `shop.UpdateDetails(shopDto.Name, shopDto.Description, (PriceRange)shopDto.PriceRange)` immediately after the CoffeeShop constructor already sets Name and PriceRange on line 25. Remove line 26 (the UpdateDetails call) entirely. The constructor `new CoffeeShop(creatorId, shopDto.Name, (PriceRange)shopDto.PriceRange, moderationId)` already sets Name and PriceRange. Description is not set by the constructor — to preserve the Description assignment, keep UpdateDetails but only if it sets Description; otherwise check whether CoffeeShop has a SetDescription or UpdateDescription method. If UpdateDetails is the only way to set Description, keep the call but note the constructor redundancy is acceptable — the concern is only the duplicate Name/PriceRange assignment. In practice: the constructor sets Name and PriceRange, while UpdateDetails also sets Description. Remove the call only if Description can be set another way, OR keep it and remove the explicit Name/PriceRange from the constructor invocation if the CoffeeShop constructor signature allows it. The simplest correct fix: keep the UpdateDetails call (which properly sets Description) and change the constructor call to not pass Name/PriceRange if there is an overload without them — or, if no such overload exists, simply remove the UpdateDetails call and call `shop.UpdateDetails` only for Description separately. Check the CoffeeShop constructor and UpdateDetails signatures before editing. The key goal: each value (Name, PriceRange, Description) is set exactly once, not twice.
  </action>
  <verify>
    <automated>grep -n "UpdateDetails" CoffeePeek.Shops.Application/Services/CreateShopFromModerationService.cs</automated>
  </verify>
  <done>Line 26 no longer immediately duplicates values already set by the constructor. Name and PriceRange are set exactly once on the newly created CoffeeShop. dotnet build CoffeePeek.slnx succeeds with no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Replace InvalidOperationException with ValidationException in UserFavoriteService (TD-04)</name>
  <files>CoffeePeek.Shops.Application/Services/UserFavoriteService.cs</files>
  <read_first>
    - CoffeePeek.Shops.Application/Services/UserFavoriteService.cs (full file — lines 18-25 and 50-57)
    - CoffeePeek.Shared.Kernel/Exceptions/ValidationException.cs (confirm constructor signature)
  </read_first>
  <action>
    In UserFavoriteService.cs there are four guard clauses throwing InvalidOperationException:
    - AddToFavoritesAsync line 19: `throw new InvalidOperationException("UserId cannot be empty")`
    - AddToFavoritesAsync line 24: `throw new InvalidOperationException("CoffeeShopId cannot be empty")`
    - RemoveFromFavoritesAsync line 51: `throw new InvalidOperationException("UserId cannot be empty")`
    - RemoveFromFavoritesAsync line 56: `throw new InvalidOperationException("CoffeeShopId cannot be empty")`

    Replace all four with `throw new ValidationException("UserId cannot be empty")` and `throw new ValidationException("CoffeeShopId cannot be empty")` respectively. ValidationException is in namespace `CoffeePeek.Shared.Kernel.Exceptions` — add the using if not already present (check existing usings). Do not change any other logic. GlobalExceptionHandler maps ValidationException to HTTP 400; InvalidOperationException maps to HTTP 500.
  </action>
  <verify>
    <automated>grep -c "InvalidOperationException" CoffeePeek.Shops.Application/Services/UserFavoriteService.cs</automated>
  </verify>
  <done>grep -v '^//' CoffeePeek.Shops.Application/Services/UserFavoriteService.cs | grep -c "InvalidOperationException" returns 0. All four guard clauses use ValidationException. dotnet build CoffeePeek.slnx succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Delete empty CoffeeShopRepository.cs (TD-06)</name>
  <files>CoffeePeek.Shops.Infrastructure/Services/CoffeeShopRepository.cs</files>
  <read_first>
    - CoffeePeek.Shops.Infrastructure/Services/CoffeeShopRepository.cs (confirm it is effectively empty — 1 line)
  </read_first>
  <action>
    The file CoffeePeek.Shops.Infrastructure/Services/CoffeeShopRepository.cs contains only 1 line (a namespace or blank) with no actual implementation. Delete the file using the Bash tool: `rm CoffeePeek.Shops.Infrastructure/Services/CoffeeShopRepository.cs`. Before deleting, verify no other file imports or references CoffeeShopRepository from this namespace: run `grep -rn "CoffeeShopRepository" CoffeePeek.Shops.Infrastructure/ --include="*.cs"` and `grep -rn "CoffeeShopRepository" CoffeePeek.ShopsService/ --include="*.cs"`. If there are references, do NOT delete — instead report what is referencing it. If no references: delete the file.
  </action>
  <verify>
    <automated>test ! -f CoffeePeek.Shops.Infrastructure/Services/CoffeeShopRepository.cs && echo "deleted" || echo "still exists"</automated>
  </verify>
  <done>CoffeeShopRepository.cs no longer exists in the filesystem. dotnet build CoffeePeek.slnx succeeds with no new errors (no dangling references).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→Application | UserId/CoffeeShopId arrive from controller layer after gateway JWT extraction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | UserFavoriteService guard clauses | accept | ValidationException maintains same guard logic; only exception type changes — no security regression |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After all three tasks:
1. `dotnet build CoffeePeek.slnx` — zero new errors or warnings
2. `grep -rn "InvalidOperationException" CoffeePeek.Shops.Application/Services/UserFavoriteService.cs` — no output
3. `test ! -f CoffeePeek.Shops.Infrastructure/Services/CoffeeShopRepository.cs` — exits 0
4. `grep -n "UpdateDetails" CoffeePeek.Shops.Application/Services/CreateShopFromModerationService.cs` — if present, Name/PriceRange appear only once across constructor + UpdateDetails combined
</verification>

<success_criteria>
- TD-02: CoffeeShop Name and PriceRange are each assigned exactly once in CreateShopFromModerationService
- TD-04: UserFavoriteService throws ValidationException for empty Guid inputs; HTTP response is 400 not 500
- TD-06: CoffeeShopRepository.cs deleted; build passes
</success_criteria>

<output>
Create `.planning/phases/01-tech-debt-cleanup/01-01-SUMMARY.md` when done
</output>
